# Stabilization Plan — EVETIS WB Analytics

> План стабилизации проекта **EVETIS WB Analytics** после первичного аудита кода.
> Опирается на `docs/current_code_map.md`, учитывает `docs/data_source_policy.md`. Документ **не переписывает архитектуру** — он фиксирует безопасный порядок работы до того, как проект пойдёт в развитие.
> Статус: проект на проверку. Версия системы на момент написания: `1.0.3-mvp`.
> Обновление: зафиксирована целевая модель источников данных **API-first / API-only**.

---

## 1. Цель стабилизации

Зафиксировать текущее работающее состояние и убрать риск тихой поломки данных до начала любого развития, с учётом перехода на целевую модель **API-first / API-only**.

Конкретно стабилизация должна обеспечить:

- один понятный путь для финансов, CLEAN и юнит-экономики (вместо нескольких параллельных);
- единый производственный источник регулярной аналитики — **WB API**;
- защиту боевых данных от тестовых и разрушительных функций;
- обязательные контрольные сверки перед изменением ключевых листов;
- ясность, какие функции «живые», а какие — legacy/заморожены.

Стабилизация **не** включает: новую архитектуру, новые функции, удаление старого кода (в т.ч. Excel/Drive), доработку заглушек API.

---

## 2. Главный принцип: сначала стабильность, потом развитие

Порядок работы строго последовательный:

1. **Сначала зафиксировать и проверить** то, что уже работает, и проверить покрытие WB API.
2. **Только потом** развивать (API заказов/продаж/рекламы, PNL/дашборд, рефакторинг).

Из этого следуют правила-ограничители на период стабилизации:

| Правило | Формулировка |
|---|---|
| Минимальность | Любое изменение — минимальное, понятное, проверяемое, обратимое (как в `CLAUDE.md`). |
| Без переписывания | Архитектура не меняется, код не переписывается. |
| Без патчей поверх патчей | Новые патчи не накладываются на нерешённые дубли. |
| Один путь | По каждому процессу выбирается один канонический маршрут, остальные замораживаются. |
| Один источник | Производственный источник регулярной аналитики — только WB API; Excel/Drive — сверка/fallback. |

---

## 3. Канонический финансовый путь

**Решение: `RAW_WB_FINANCE` — главный источник финансового факта WB для текущей модели P&L. Производственное наполнение этого слоя в целевой модели — только WB API.**

Каноническим является именно **слой `RAW_WB_FINANCE`**. В целевой модели **API-first / API-only** его наполняет WB API. Excel/Drive больше **не считается целевым производственным источником** и допускается только как временная историческая сверка, аудит расхождений, fallback при недоступности API и архивный источник для старых периодов.

Целевой путь данных:

```
WB API  →  RAW  →  CLEAN  →  ЮНИТ / PNL  →  DASHBOARD
              ↑
Excel/Drive ─ ┘  (только сверка / аудит расхождений / fallback / архив старых периодов — вне регулярного потока)
```

| Элемент | Канон | Примечание |
|---|---|---|
| Источник финансового факта | `RAW_WB_FINANCE` | Главный источник финансового факта WB для текущей модели P&L |
| Производственное наполнение | WB API (`importWbFinanceFromApi`, `Wbfinanceloader`) | Единственный производственный источник регулярной аналитики |
| Excel/Drive | сверка / аудит / fallback / архив | Не целевой производственный источник; вне регулярного P&L |
| Лог и контроль | `IMPORT_LOG_FINANCE`, `computeFinanceControl_` | Контрольные суммы за период |

В целевом производственном контуре финансовый факт берётся из `RAW_WB_FINANCE`, наполненного через WB API, и нигде не подменяется ручными формулами поверх RAW. До полного перехода Excel/Drive-данные допускаются только как сверка, аудит расхождений, fallback или архив старых периодов.

---

## 4. Канонический CLEAN-путь

**Решение: `CLEAN_WB_DAILY` остаётся промежуточным слоем. Но до развития нужно выбрать ОДНУ каноническую схему — 15 колонок или расширенную.**

CLEAN наследует источник от RAW: в целевой модели — данные WB API.

Сейчас в коде сосуществуют две ширины (карта кода, §8, §10):

| Схема | Где задаётся | Кто пишет |
|---|---|---|
| 15 колонок (`CLEAN_HEADERS_NEW_`) | `Testrawtoclean` | `resetCleanWbDailyStructure`, тестовый контур |
| Расширенная (`CLEAN_HEADERS_FULL_`) | `Wbfinance` | `buildCleanWbDailyWithFinance` |

Риск: при разной ширине одна функция может срезать колонки данных другой.

Порядок действий на стабилизацию (без изменения кода прямо сейчас):

1. Выбрать одну каноническую схему как стандарт.
2. До выбора — не запускать переключение между схемами на боевой таблице.
3. После выбора — зафиксировать решение в `CHANGELOG.md`, вторую схему пометить как legacy.

До принятия решения CLEAN-путь считается «заморожен по схеме»: пересборка допустима только в рамках уже выбранной ширины с контролем сумм.

---

## 5. Канонический путь юнит-экономики / PNL

**Решение: временно не развивать все три версии юнит-отчёта одновременно.**

Текущее состояние (карта кода, §7): три билдера пишут на листы `ЮНИТ_MM_YYYY`:

| Версия | Функция | Файл | Статус на стабилизацию |
|---|---|---|---|
| v10 (новая) | `buildMonthlyUnitReportV10` | `MonthlyUnitReport_v10` | Предварительный кандидат на канон (хранение по `RAW_WB_STORAGE`, реклама по `RAW_WB_ADS`, встроенные сверки) |
| «с финансами» | `buildMonthlyUnitReportWithFinance` | `Wbfinance` | Заморозить как legacy |
| v1.2 (старая) | `buildMonthlyUnitReport` | `Monthlyunitreport` | Заморозить как legacy |

`buildMonthlyUnitReportV10` рассматривается **только как предварительный кандидат** на каноническую версию. **v10 не утверждена как каноническая до проведения контрольной сверки на выбранном периоде** (см. §8 и §11). До этой сверки v10 не получает статус канона, а две другие версии остаются в коде как legacy.

Принципы:

- Отдельного билдера `PNL_TOTAL`/`PNL_SKU` нет — P&L живёт внутри юнит-отчётов; на стабилизацию это сохраняем как есть.
- Развивается **только одна** версия — после её утверждения. Остальные не дорабатываются, но и не удаляются.
- Любое наполнение `ЮНИТ_MM_YYYY` проходит встроенную сверку CLEAN → ЮНИТ.

Выбор канонической версии фиксируется отдельным решением и записью в `CHANGELOG.md`.

---

## 6. Что временно замораживаем

«Заморозка» = функция остаётся в коде, помечается как legacy, не дорабатывается и не запускается в регулярном рабочем процессе.

| Объект заморозки | Где | Причина |
|---|---|---|
| Два неканонических билдера юнита | `Monthlyunitreport`, `Wbfinance` | Оставляем одну версию (§5) |
| Неканоническая схема CLEAN | `Testrawtoclean` или `Wbfinance` (после выбора) | До выбора одной ширины (§4) |
| Дублирующие меню юнита | `addUnitReportMenu` / `addUnitReportMenuV10` | Во избежание запуска не той версии |
| Тестовый контур | `fillTestRawDataForClean`, `fillTestFinanceRawData`, `resetCleanWbDailyStructure` | Не для боевой таблицы (§7, §12) |
| Заглушки API | меню «📡 API WB», «📊 Расчёты» | Это развитие, не стабилизация |

Excel/Drive-импорт переводится в статус **legacy/fallback** (не удаляется):

| Модуль / функции | Тип | Роль после перехода |
|---|---|---|
| `Wbfinanceimportfromdrive` | Excel/Drive финансы | legacy/fallback: сверка / аудит / архив |
| `Wbfinancemonthclose` (Drive-only) | Drive финансы | legacy/fallback: сверка / архив старых периодов |
| `WbStorageImport` | Excel хранение | legacy/fallback: сверка / архив |
| Drive-only загрузчики (в т.ч. Drive-часть `Wbstoragefolderloader`) | Drive | legacy/fallback (API-часть остаётся производственной) |
| Функции с хардкодом периода 18–24.05.2026 (`importWbFinanceReports_2026_05_18_24`, `importWbStorageReport_2026_05_18_24`, `updateWbFinanceMay2026FromDriveOnly`, `previewWbFinanceMay2026DriveFiles`) | Excel/Drive, хардкод | legacy/fallback: только для контрольной сверки периода |

Важно: **до отдельного решения старые функции не удаляются** — только помечаются как legacy/заморожены.

---

## 7. Опасные функции, которые нельзя запускать без подтверждения

Из карты кода (§11, §13). Запуск только осознанно, на копии или с контрольными суммами до/после.

| Функция | Файл | Эффект |
|---|---|---|
| `resetCleanWbDailyStructure` | `Testrawtoclean` | Полная очистка `CLEAN_WB_DAILY` |
| `fillTestRawDataForClean` | `Testrawtoclean` | Перезапись RAW тестовыми данными |
| `fillTestFinanceRawData` | `Wbfinance` | Перезапись финансового RAW тестовыми расходами |
| `buildCleanWbDailyWithFinance` | `Wbfinance` | Перезапись заголовков + удаление колонок CLEAN |
| `removeAllFinanceApiRows` | `Wbfinancemonthclose` | Удаление API-строк RAW_WB_FINANCE + пересборка CLEAN |
| `clearCleanRowsOutsidePeriod_` / `clearFinanceOwnPeriod_` | `Cleanwbdailyperiod` / `Wbfinanceloader` | Удаление строк за/вне периода |
| `setupWorkbook` / `createAllSheets` | `Code.gs` | Обновление структуры всех листов, удаление лишних колонок |
| `installCostEditTrigger` | `CostManagement` | Включает авто-запись в COST_HISTORY по каждому редактированию |

**Правило:** тестовые функции на боевой таблице не запускаются вообще; разрушительные функции — только с подтверждением и контрольными цифрами.

---

## 8. Контрольные цифры и сверки

**Решение: `RAW_WB_FINANCE`, `CLEAN_WB_DAILY`, `SKU_MASTER`, `COST_HISTORY` и юнит-отчёты не меняются без контрольных сумм.**

Опираемся на уже существующие механизмы (карта кода, §15) — новые писать не нужно:

| Механизм | Что сверяет |
|---|---|
| `IMPORT_LOG_FINANCE` | Итоги загрузки финансов: строки, реализация, к перечислению, логистика, хранение, удержания, «после расходов WB» |
| `IMPORT_LOG_STORAGE` | Итоги загрузки хранения |
| `computeFinanceControl_` | Контроль сумм за период из RAW_WB_FINANCE |
| Сверка CLEAN → ЮНИТ | Встроена в билдер юнита (расхождения → `ERRORS_CONTROL`) |
| `verifyStorageReconciliation` | Хранение: RAW_WB_STORAGE vs RAW_WB_FINANCE по пересечению дат |
| `writeReconciliationErrors_` | ADS vs «WB Продвижение» из финотчёта |
| `auditSkuCostLinks`, `auditSkuAndCost` | Связка SKU_MASTER ↔ COST_HISTORY, дубли `is_current` |
| `ERRORS_CONTROL` | Общий журнал расхождений |

Минимальный обязательный набор контрольных цифр перед любым изменением ключевого листа: продажи (шт), реализация WB, к перечислению WB, логистика, хранение, удержания, «после расходов WB», себестоимость продаж, прибыль.

**Важно про источник эталона:** цифры, снятые с Excel/Drive-отчётов, **не фиксируются как окончательный эталон**. Они допустимы только как временная сверка до тех пор, пока не будет получен контрольный период в режиме **API-only**. Окончательный эталон должен быть зафиксирован в будущем `docs/control_values.md` только после подтверждения контрольных значений по данным WB API.

---

## 9. Первый приоритет стабилизации

**Проверить покрытие WB API и построить API-only карту данных.** (Заменяет прежний первый приоритет.)

| Шаг | Действие |
|---|---|
| 1.1 | Провести инвентаризацию покрытия WB API по блокам: заказы, продажи, возвраты, остатки, финансовый отчёт реализации, логистика, хранение, удержания, платная приёмка, реклама, рекламные расходы, детализация рекламы по дате/nmId/advertId, себестоимость из внутренних справочников. Статус по умолчанию — «требует проверки». |
| 1.2 | Построить API-only карту данных: целевой путь `WB API → RAW → CLEAN → ЮНИТ / PNL → DASHBOARD`, с указанием, какой блок каким API-методом покрывается. |
| 1.3 | Разобрать **все показатели P&L по источникам** (таблица ниже). |
| 1.4 | Зафиксировать пробелы покрытия (что API пока не отдаёт) как явные `TBD`, не закрывая их Excel/Drive в обход политики. |
| 1.5 | Excel/Drive-модули (§6) пометить как legacy/fallback без удаления; результат — в `CHANGELOG.md`. |

Разбор показателей P&L по источникам (шаблон, заполняется при инвентаризации):

| Показатель P&L | API-метод (источник) | Уровень детализации (кабинет / операция / дата / SKU / nmId / advertId) | Надёжная привязка к SKU? | Если к SKU нельзя — как учитывать |
|---|---|---|---|---|
| Заказы | TBD | TBD | TBD | TBD |
| Продажи | TBD | TBD | TBD | TBD |
| Возвраты | TBD | TBD | TBD | TBD |
| Реализация WB | TBD | TBD | TBD | TBD |
| К перечислению WB | TBD | TBD | TBD | TBD |
| Логистика | TBD | TBD | TBD | TBD |
| Хранение | TBD | TBD | TBD | TBD |
| Удержания | TBD | TBD | TBD | TBD |
| Платная приёмка | TBD | TBD | TBD | TBD |
| Реклама / рекламные расходы | TBD | TBD | TBD | TBD |
| Себестоимость | внутренние справочники (`COST_HISTORY`/`BUNDLES`) | SKU / nmId | TBD | по утверждённому правилу |

Правило для непривязываемых показателей: если показатель нельзя надёжно отнести к SKU, он учитывается одним из способов — **кабинетный расход**, **отдельный блок** или **распределение по утверждённому правилу** (с явной пометкой «оценка»). Способ фиксируется отдельным решением.

**Особо про рекламу:** рекламные расходы **нельзя относить к SKU как точный факт**, если WB API не даёт надёжную связку `date + nmId + advertId + spend`. До подтверждения такой связки реклама по SKU — только оценка либо кабинетный блок.

---

## 10. Второй приоритет стабилизации

Зафиксировать финансовый факт из API и выбрать канонический CLEAN.

| Шаг | Действие |
|---|---|
| 2.1 | Подтвердить, что `RAW_WB_FINANCE` наполняется из WB API и является единственным производственным источником финансового факта; ручных формул поверх RAW нет. |
| 2.2 | Снять контрольные суммы по `RAW_WB_FINANCE` на API-данных за период 18–24.05.2026 либо за другой период, по которому API-данные доступны полно и подтверждены. Excel/Drive — только для параллельной сверки и аудита расхождений. |
| 2.3 | Выбрать одну схему `CLEAN_WB_DAILY` (15 или расширенная); решение — в `CHANGELOG.md`, вторую схему пометить как legacy. |
| 2.4 | Сверить пересобранный CLEAN с контрольными суммами из §8 (CLEAN ↔ RAW_WB_FINANCE) и зафиксировать канонический билдер CLEAN. |

---

## 11. Третий приоритет стабилизации

Выбрать одну версию юнит-экономики / PNL.

| Шаг | Действие |
|---|---|
| 3.1 | Провести контрольную сверку `buildMonthlyUnitReportV10` на выбранном периоде (на API-данных); только при успешной сверке утвердить её как каноническую. |
| 3.2 | Две другие версии пометить как legacy/заморожены, оставить в коде. |
| 3.3 | Привести меню к одному пункту запуска канонической версии. |
| 3.4 | Прогнать сверку CLEAN → ЮНИТ и сверки хранения/рекламы; расхождения — в `ERRORS_CONTROL`. |

---

## 12. Что пока не делать

| Не делать | Почему |
|---|---|
| Запускать тестовые функции на боевой таблице | Затирают RAW / CLEAN |
| Удалять старые функции (в т.ч. Excel/Drive) | До отдельного решения только пометка legacy |
| Развивать все три версии юнита одновременно | Только одна каноническая (§5) |
| Менять структуру колонок ключевых листов без сверки | Нужны контрольные суммы (§8) |
| Реализовывать заглушки API без проверки покрытия | Сначала инвентаризация покрытия WB API (§9) |
| Распределять рекламу по SKU как точный факт | Только при надёжной связке `date + nmId + advertId + spend`; иначе «оценка» |
| Накладывать новые патчи поверх нерешённых дублей | Усложняет стабилизацию |
| Строить новый P&L на Excel/Drive | Excel/Drive — не целевой производственный источник |
| Считать Excel/Drive контрольные цифры окончательным эталоном | Эталон подтверждается по API-only периоду |
| Распределять неотнесённые расходы вручную без утверждённого правила | Способ учёта должен быть зафиксирован отдельным решением |

---

## 13. Правила для следующих изменений

| Правило | Формулировка |
|---|---|
| Перед кодом — описание | Сначала: что меняется, какие листы затронуты, риск поломки, контрольные цифры, и только потом код (по `CLAUDE.md`). |
| Один канон | По финансам, CLEAN и юниту — один путь; альтернативы заморожены. |
| Один производственный источник | Регулярная аналитика — только WB API; Excel/Drive — сверка/fallback/архив. |
| Контрольные суммы обязательны | `RAW_WB_FINANCE`, `CLEAN_WB_DAILY`, `SKU_MASTER`, `COST_HISTORY`, юнит-отчёты — только со сверкой. |
| Эталон — по API | Окончательные контрольные цифры подтверждаются на API-only периоде, не на Excel/Drive. |
| Legacy вместо удаления | Старые функции (в т.ч. Excel/Drive) помечаются, не удаляются, без отдельного решения. |
| Реклама по SKU — не факт без связки | Только при надёжной связке `date + nmId + advertId + spend`, иначе помечать как «оценка». |
| Неотнесённые расходы — по правилу | Кабинетный расход / отдельный блок / распределение по утверждённому правилу. |
| Запись в `CHANGELOG.md` | Каждое изменение: дата, что, зачем, какие листы, как проверить. |
| Обратимость | Любое изменение должно быть откатываемым. |

---

*Документ фиксирует порядок стабилизации и целевую модель источников (API-first / API-only); кода не меняет, Excel/Drive-функции не удаляет. Канонические выборы (схема CLEAN, версия юнита, способ учёта непривязываемых расходов) принимаются отдельными решениями с записью в `CHANGELOG.md`.*
