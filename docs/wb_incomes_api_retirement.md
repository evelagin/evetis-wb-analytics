# Снятие метода `/api/v1/supplier/incomes` и поиск источника истории поставок

Дата: 17.08.2026
Статус: **расследование завершено.** Источник выбран, семантика полей
установлена, ветка нулей закрыта первичными документами WB — см. разделы 12 и
13. Проектирование вынесено в `docs/wb_supplies_raw_design.md`.

Документ описывает четыре пробы, их результаты и то, как из них получилась
семантика источника. Наблюдения и интерпретации разделены намеренно; каждый
раунд ревю аудитора зафиксирован отдельным подразделом.

---

## 1. Задача

История поставок и приёмки на склады WB **не загружалась ни разу**. В BigQuery
нет ни `RAW_WB_INCOMES`, ни аналога. Без неё не строится материальный баланс
«поступило → продано → возвращено → осталось → утрачено» с сентября 2024, а он
нужен под фиксацию потерь и под заявки на компенсацию.

Штатным источником по документации WB был `GET /api/v1/supplier/incomes`
(категория токена «Статистика»).

---

## 2. Наблюдения

### 2.1 Проба №1 — `probeWbIncomes`, 17.08.2026 16:10

| Запрос | HTTP | Тело |
|---|---|---|
| `GET statistics-api /api/v1/supplier/incomes?dateFrom=2024-01-01T00:00:00` | 404 | `{"detail":"Please consult the …/api-information","origin":"ag-statistics","title":"path not found"}` |
| `GET statistics-api /api/v2/supplier/incomes?…` | 404 | то же |
| `GET supplies-api /api/v1/warehouses` | 403 | `scope is not allowed for this resource` |

### 2.2 Проба №2 — `probeWbIncomes2`, 17.08.2026 16:58

| № | Запрос | HTTP | Существенное из тела |
|---|---|---|---|
| A1 | `GET statistics-api /ping` | **200** | `{"TS":"2026-08-17T13:58:02Z","Status":"OK"}` |
| A2 | `GET statistics-api /api/v1/supplier/stocks?dateFrom=2019-06-20` | 404 | `This method is deprecated. Link: …/release-notes?id=494` |
| A3 | `GET statistics-api /api/v1/supplier/incomes?dateFrom=2024-01-01T00:00:00` | 404 | `path not found` |
| A4 | `GET statistics-api /api/v1/supplier/incomes` | 404 | `path not found` |
| B1 | `GET seller-analytics-api /api/v1/acceptance_report?dateFrom=2026-07-17&dateTo=2026-08-17` | **200** | `{"data":{"taskId":"19745704-…"}}` |
| B2 | `POST` тот же адрес | 405 | `method not allowed`, `origin: ag-contentanalytics` |
| B3 | `GET …/acceptance_report?dateFrom=2024-09-01&dateTo=2024-09-30` | **200** | `{"data":{"taskId":"b86e327f-…"}}` |
| C1 | `GET supplies-api /ping` (токен `WB_TOKEN_SUPPLIES`) | **200** | `Status: OK` |
| C2 | `GET supplies-api /api/v1/warehouses` | 404 | `This method is **temporarily** disabled. Link: …/release-notes?id=570` |
| C3 | `GET supplies-api /api/v1/supplies?limit=50&offset=0` | 405 | `method not allowed` |

---

## 3. Интерпретация

### 3.1 Метод `incomes` снят, а не переехал

Шлюз различает два разных отказа, и разница содержательная:

- на снятый, но известный ему метод (`supplier/stocks`) он отвечает **именной
  пометкой** `This method is deprecated` со ссылкой на релиз-ноту;
- на `supplier/incomes` — безымянным `path not found`.

То есть маршрут `incomes` удалён целиком, вместе с заглушкой о депрекации. Версии
`v2` не существует: она дала тот же безымянный отказ от того же шлюза.
`A1 = 200` доказывает, что токен и шлюз при этом полностью рабочие.

**Вывод: восстановить прежний источник нечем. Нужен другой метод.**

### 3.2 Отчёт о приёмке — задачный, и глубина 2024 года не отвергнута

`GET /api/v1/acceptance_report` — это **не чтение, а создание задачи**: он
вернул `taskId`. Проба №2 приняла его за синхронное чтение, не нашла массива и
ушла в `POST`, где получила 405. Ошибка пробы, не отказ WB.

🔑 Отдельно ценно: `taskId` выдан **и на окно сентября 2024**. Само по себе это
не доказывает, что данные за 2024 есть — задача могла бы вернуться пустой. Но и
не отвергает: отказа по глубине шлюз не дал.

Документация WB для этого метода указывает `POST` на создание задачи. Живой шлюз
принимает `GET` и отвергает `POST`. **Верим шлюзу, не документации** — и это
правило стоит держать в голове на весь контур WB.

### 3.3 Шлюз поставок открыт, но список поставок — POST

`WB_TOKEN_SUPPLIES` работает: `/ping` = 200, категория выдана верно, прежний 403
закрыт.

`/api/v1/supplies` ответил 405 на `GET` — по документации метод **POST**, с
телом фильтра (`dates`, `statusIDs`) и `limit`/`offset` в строке запроса. Это
ошибка пробы.

`/api/v1/warehouses` отключён **временно** (релиз-нота 570). Формулировка WB
отличается от `deprecated` у остатков. Совпадение по времени с переходом на
отгрузку в ПВЗ с 15.08 напрашивается, но **это догадка, а не установленный
факт** — на выбор источника она не влияет.

### 3.4 🔴 Сопутствующий риск: legacy-семейство `/api/v1/supplier/*` разбирают

Уже сняты `stocks` (23.06, релиз-нота 494) и `incomes`. В том же семействе и на
том же шлюзе работает **боевой загрузчик заказов**:

- `apps-script/WbOrdersLoader` → `statistics-api` + `/api/v1/supplier/orders`;
- продажи и возвраты — соседний загрузчик того же семейства.

Сегодня они работают, суточный прогон 17.08 прошёл штатно. Но это та же полка,
с которой WB уже снял две позиции: отключение `supplier/stocks` с 23.06.2026 WB
объявлял официально — это не гипотеза о возможной миграции, а начатый демонтаж
старого Statistics API. При этом `orders` и `sales` в актуальном разделе
документации присутствуют, а `incomes` из него уже исчез.

Нужен сторож, который заметит изменение раньше, чем встанет витрина. Добавлен
блоком D в пробу №3 — и сделан **контрактным**, а не проверкой доступности:
`200` с валидной структурой, `404 deprecated`, `404 path not found`, `401/403`,
`429`, `5xx` и структурный дрейф JSON — это семь разных состояний, и uptime-чек
различает из них два.

🔑 Формулировка тоже важна: `200` означает «маршрут сейчас операционно доступен»
и ничего не говорит о будущей дате отключения.

### 3.5 Ошибка в пробе №2, которую надо назвать

Контролем «шлюз жив» я выбрал `/api/v1/supplier/stocks`. Метод оказался снятым —
причём в нашем же `docs/wb_api_test_plan.md` отключение стояло на 23 июня, то
есть проверяемый ответ лежал в репозитории. Контроль сработал случайно, через
`/ping`. Урок тот же, что и в [analysis-method-lessons-2026-08-17]: сверяться с
собственными документами до того, как строить проверку.

---

## 4. Два кандидата на источник

| | Отчёт о приёмке | Список поставок |
|---|---|---|
| Метод | `GET /api/v1/acceptance_report` → `/tasks/{id}/status` → `/download` | `POST /api/v1/supplies`, `POST /api/v1/supplies/{ID}/goods` |
| Хост / категория | `seller-analytics-api`, «Аналитика» | `supplies-api`, «Поставки» |
| Доступ | открыт | открыт (токен заведён 17.08) |
| Поля | `incomeId`, `nmID`, `shkCreateDate`, `giCreateDate`, `count`, `subjectName`, `total` | поставка: `supplyID`, `createDate`, `supplyDate`, `factDate`, `statusID`; состав: `barcode`, `vendorCode`, `nmID`, `techSize`, `quantity`, `acceptedQuantity`, `unloadingQuantity` |
| Что даёт по сути | факт приёмки по товару и дате | **кандидата** на пару `quantity` / `acceptedQuantity` («передано / принято») по SKU; семантика подтверждается сверкой с шестью поставками июля |
| Чего нет | склада, `supplierArticle` | склада в списке — проверяется карточкой поставки |
| Ограничения | окно 31 день → около 24 запросов на два года при лимите 1 запрос в минуту | глубина назад неизвестна |
| Риск | метод про **платную** приёмку; при бесплатной приёмке строк может не быть вовсе | это план поставки из кабинета, а не акт приёмки склада |

### 4.1 Кандидаты не конкурируют — выбирать между ними искусственно не нужно

Наиболее вероятная и наиболее полезная конфигурация — оба источника сразу, в
разных ролях:

- **Список поставок — первичный реестр объектов поставок**: `supplyID`, `nmID`,
  `quantity`, `acceptedQuantity`, даты и статус. 🔴 Реестр объектов, а не факта:
  сам по себе он содержит и несостоявшиеся поставки. Факт физической поставки
  определяется отдельно — по `statusID` и непустому `factDate`.
- **Отчёт о приёмке — независимый контрольный источник**, если в нём окажутся
  наши приёмки: своя нумерация `incomeId` и своя дата.

Для задачи с 3 492 потенциально утраченными единицами два независимых источника
сильнее одного: расхождение между ними само по себе становится доказательством,
а совпадение — подтверждением. Поэтому проба №3 не выбирает победителя, а
устанавливает, что каждый из них реально отдаёт.

---

## 5. Что делаем дальше

1. Проба №3 — `apps-script/WbIncomesShapeProbe3.gs`, две функции:
   `probeAcceptance3()` (блок B, правильный задачный контур, свежее окно и
   сентябрь 2024) и `probeSupplies3()` (блоки C и D).
2. По журналу выбираем источник и **только после этого** проектируем загрузчик:
   RAW-таблица, ключ, watermark, канонический вью. До выбора код не пишется.
3. Сторож legacy из блока D переносим в регулярный прогон отдельной задачей.

## 6. Контрольные цифры для приёмки результата

- Число поставок за период 06.07–28.07.2026 должно совпасть с шестью поставками,
  которые владелец помнит поимённо: Тула, Волгоград, Сарапул, Владимир, Самара,
  Екатеринбург.
- `quantity` по этим поставкам должен согласоваться с объёмом, ушедшим со склада
  по внутреннему учёту.
- Материальный баланс. 🔴 Слово «возврат» здесь обозначает **две противоположные
  по знаку операции**, и смешивать их нельзя:

  ```
  остаток на WB = принято
                − продано
                + возврат покупателя на склад WB      (знак «плюс»)
                − возврат товара с WB продавцу        (знак «минус»)
                − списания и утраты
                ± внутренние перемещения
  ```

  В нашем контуре `V_WB_SALES_RETURNS` покупательские возвраты и вывоз товара
  продавцу разными полями не разделены — это надо установить до сверки, иначе
  ошибка знака удвоит расхождение.

  Ожидаемая сходимость: 4 796 ед на WB, из них 3 492 утрачено. Невязка этого
  равенства и есть искомая величина «что потерялось».

## 7. Что этот документ не утверждает

### 7.1 Действующие ограничения

- Что `acceptedQuantity` — окончательное актовое количество приёмки. Это
  кандидат на пару «передано / принято». Открыто: в какой момент WB фиксирует
  поле и меняется ли оно задним числом.
- Что `acceptedQuantity = 0` означает непринятый товар. У поставки 04.09.2024
  поле равно нулю при `quantity` 2400, и та же поставка присутствует в отчёте о
  приёмке. Ноль может быть и незаполненным задним числом полем, и настоящим
  нулём приёмки — различить это пока нечем.
- Что `incomeId` из отчёта о приёмке равен `supplyID` из списка поставок как
  общее правило. Совпадение проверено на одной паре, 22971287.
- Что `count` в отчёте о приёмке — это количество товара. Строка с `count 4`,
  `nmID 0` и `total 20000` этому противоречит.
- Что склад из карточки поставки — это текущее расположение остатка. Это точка
  приёмки; после неё товар мог быть перемещён.
- Что пустая выгрузка отчёта о приёмке за свежее окно объясняется бесплатной
  приёмкой. Причина не установлена.
- Что `/api/v1/warehouses` отключён именно из-за перехода на ПВЗ.
- Что `200` на заказах и продажах означает их безопасность. Это означает только
  «маршрут сейчас операционно доступен» и ничего не говорит о будущей дате
  отключения.

### 7.2 Ограничения, закрытые пробой №3

Здесь то, что раньше стояло в списке выше и с тех пор проверено. Оставлено для
истории — чтобы через месяц не переоткрывать вопрос заново.

| Было под сомнением | Чем закрыто |
|---|---|
| данные за 2024 в отчёте о приёмке не прочитаны | прочитаны: окно сентября 2024 дало 6 строк, раздел 9.1 |
| `supplies` не доказано отдаёт историю до сентября 2024 | `createDate` от 03.09.2024, `factDate` от 04.09.2024, раздел 9.2 |
| первая страница `/api/v1/supplies` может быть не всей историей | 131 строка при `limit` 1000 — выборка не усечена, раздел 9.2 |

---

## 8. Ревю аудитора, 17.08.2026

Ревю принято, правки внесены в пробу и в этот документ:

| Замечание | Что сделано |
|---|---|
| Вывод «нет строк → приёмка бесплатная» недоказуем | подсказка в журнале переведена в нейтральную форму, добавлена в раздел 7 |
| Пагинация `supplies` не контролируется | проба печатает признак усечения; при `N == limit` «самая старая» помечается недоказанной |
| `factDate`/`supplyDate`/`createDate` слиты через `\|\|` | границы считаются по каждому полю отдельно, опорным берётся `factDate`, подмена пишется в журнал как реконструкция |
| «Прямой ответ отправили/приняли» — слишком сильно | переформулировано в «кандидат», проверка перенесена на сверку с шестью поставками |
| Знак возврата в контрольной формуле | формула переписана с разделением покупательского возврата и вывоза товара продавцу |
| Блок D — uptime, а не контракт | сделан contract canary: классифицирует `200` + структуру, `deprecated`, `path not found`, `401/403`, `429`, `5xx`, дрейф JSON |

Порядок работ подтверждён: проба B → проба C → сверка с шестью известными
поставками → установление семантики полей → выбор канонического источника →
и только затем RAW-таблица и загрузчик. Production-код на этом шаге не пишется.

---

## 9. Результаты пробы №3, 17.08.2026 17:58 и 18:01

### 9.1 Наблюдения, блок B — отчёт о приёмке

| Окно | Задача | Выгрузка |
|---|---|---|
| 18.07–17.08.2026 | `taskId` выдан, статус `done` | **HTTP 204, тело пустое** |
| 01.09–30.09.2024 | `taskId` выдан, статус `done` | **HTTP 200, 6 строк** |

Строки сентября 2024: поля `count`, `giCreateDate`, `incomeId`, `nmID`,
`shkCreateDate`, `subjectName`, `total`. Сумма `count` 488, сумма `total`
25 265,6. Уникальных `incomeId` — 3, уникальных `nmID` — 4.

Первая строка: `count 4`, `nmID 0`, `subjectName ""`, `total 20000`.
Последняя: `count 160`, `nmID 252442517`, `subjectName "Кремы"`, `total 272`.

### 9.2 Наблюдения, блок C — поставки

- Список: **131 поставка**, `limit` 1000, выборка **не усечена**.
- `createDate` заполнен у 131 из 131: 03.09.2024 … 22.07.2026.
- `factDate` заполнен у **108 из 131**: 04.09.2024 … 28.07.2026.
- `supplyDate` заполнен у 115 из 131.
- Статусы: `statusID 5` — 113, `statusID 1` — 16, `statusID 3` — 2.
- Поля списка: `phone`, `supplyID`, `preorderID`, `createDate`, `supplyDate`,
  `factDate`, `updatedDate`, `statusID`, `boxTypeID`, `isBoxOnPallet`.
- 🔑 **Карточка поставки содержит склад**: `warehouseID`, `warehouseName`,
  `actualWarehouseID`, `actualWarehouseName`. У поставки 22971287 — `507`,
  «Коледино», фактический тот же.
- Состав поставки — это **GET**, `POST` отвечает 405. Поля: `barcode`,
  `vendorCode`, `nmID`, `needKiz`, `tnved`, `techSize`, `color`,
  `supplierBoxAmount`, `quantity`, `readyForSaleQuantity`, `unloadingQuantity`,
  `acceptedQuantity`.

Две поставки на концах истории:

| Поставка | Факт | Позиций | `quantity` | `acceptedQuantity` |
|---|---|---:|---:|---:|
| 22971287 | 04.09.2024 | 3 | 2 400 | **0** |
| 41047776 | 28.07.2026 | 19 | 170 | **169** |

### 9.3 Наблюдения, блок D — сторож legacy

`/api/v1/supplier/orders` и `/api/v1/supplier/sales` — вердикт **ЖИВОЙ**:
`200`, массив, полный прежний набор полей, включая `warehouseName` и `incomeID`.

### 9.4 Интерпретация

**Источник найден.** `supplies-api` отдаёт всю историю с сентября 2024 одной
страницей и без усечения. Это первичный реестр **объектов** поставок: факт
физической поставки определяется отдельно, по `statusID` и непустому `factDate`.

🔑 **Склад в карточке поставки не обезличен.** Исторический контур «куда именно
приехал товар» восстанавливается поимённо, в отличие от остатков и заказов.
⚠️ Но склад поставки — это **точка приёмки**, а не текущее расположение
остатка: после приёмки товар мог быть перемещён. Смешивать эти две величины
нельзя, иначе получим ту же ошибку, что и с реконструкцией расположения.

🔑 **Два источника связываются ключом.** `incomeId` 22971287 в отчёте о приёмке
и `supplyID` 22971287 в списке поставок — одно и то же число, и даты сходятся:
`giCreateDate` 03.09.2024 против `createDate` 03.09.2024, `shkCreateDate`
04.09.2024 против `factDate` 04.09.2024.
⚠️ Это совпадение на **одной** паре. Как общее правило не постулируется —
проверяется на всей истории пробой №4.

🔴 **Главный открытый вопрос: поле `acceptedQuantity` ведёт себя по-разному.**
Ноль на поставке 2024 года и 169 из 170 на поставке июля 2026. Пока не известна
граница заполнения, материальный баланс за всю историю строить нельзя: нули
старых поставок дадут фиктивную недостачу в тысячи единиц. Это ровно та
ошибка, которой документ должен помешать.

**Отчёт о приёмке покрывает не всё.** Свежее окно пусто (204), сентябрь 2024
даёт шесть строк. Причина не назначается. Отмечу лишь, что `count 4` при
`total 20000` и `nmID 0` не согласуется с товарным количеством — похоже, часть
строк тарифицирует не единицы товара. Это гипотеза, требующая проверки.

Роли распределяются так: **`supplies` — первичный реестр объектов поставок**,
`acceptance_report` — **частичный контрольный источник** там, где строки есть.

🔴 «Реестр объектов» — не оговорка. `factDate` заполнен у 108 из 131, а
`statusID 5` — у 113. Значит в списке есть записи, которые фактом не
подтверждены. Физическая поставка — это `statusID 5` **и** непустой `factDate`;
всё остальное в материальный баланс не входит.

### 9.5 Что делает проба №4

`apps-script/WbSuppliesInventoryProbe4.gs`, четыре функции:

- `probeJuly4()` — шесть поставок июля подробно, по позициям, с тремя складами
  раздельно: назначения, транзита и `actual`. Сверка с памятью владельца.
- `probeInventory4a()` `[0…66)`, `probeInventory4b()` `[66…100)`,
  `probeInventory4c()` `[100…)` — вся история тремя диапазонами. По каждой
  поставке склад, даты, статус, `quantity` / `accepted` / `readyForSale`, плюс
  классификация заполнения `acceptedQuantity`, аудит идентификаторов и разбивка
  по складам назначения.

⚠️ Изначально диапазонов было два, по 66 записей. После прогона `4a` их стало
три — см. §11.5: прогон занял 249 с при бюджете 270 с, а пауза между запросами
выросла втрое.

Все функции только читают и печатают в Execution log. В лист не пишут
сознательно: книга упёрлась в лимит ячеек.

### 9.6 Ревю аудитора по пробе №4, 17.08.2026

Три блокера, все внесены до запуска:

| Замечание | Что сделано |
|---|---|
| Граница `acceptedQuantity` считалась по всем 131 поставке, включая `statusID 1/3` и записи без `factDate` — незавершённая поставка изобразила бы «поле не работало» | в агрегаты и в выводы о семантике идут **только** `statusID 5` **и** непустой `factDate`; остальные считаются отдельными счётчиками `nNotCompleted` / `nNoFact` / `nOtherStatus` |
| Разбивка по складам складывала все записи независимо от статуса | складской контур строится по тем же состоявшимся поставкам; несостоявшиеся помечаются `ИСКЛЮЧЕНА` и в агрегаты не идут |
| В `probeJuly4()` вернулся молчаливый fallback `factDate \|\| supplyDate \|\| createDate`, и не было верхней границы окна | фильтр строго по `factDate` при `statusID 5`, обе границы окна заданы явно (`2026-07-01`…`2026-07-31`); июльские записи без факта печатаются отдельным списком «не входят в сверку» |

Два улучшения, тоже внесены:

- Ошибка чтения API больше не превращается в данные, но исключается ровно тот
  агрегат, который испорчен: **непрочитанный состав** исключает поставку из
  количественных агрегатов и из классификации `acceptedQuantity`;
  **непрочитанная карточка** исключает её из складской разбивки, но не
  уничтожает достоверно прочитанные количества. Обе ситуации помечаются
  `DATA_MISSING` и считаются `goodsErrors` / `cardErrors`.
- Граница поля показывается не двумя датами, а классификацией завершённых
  поставок: `accepted = 0` · `0 < accepted < quantity` · `accepted = quantity` ·
  `accepted > quantity` (аномалия). Плюс отдельная проверка: встречаются ли нули
  **после** первой поставки с положительным `accepted`. Если да — печатается
  прямо: **чистой временной границы нет**, поле объясняется не только датой.

### 9.7 Второе ревю аудитора по пробе №4

| Замечание | Что сделано |
|---|---|
| 🔴 Заявленное правило `DATA_MISSING` не выполнялось для карточки: при успешном составе и `card === null` поставка попадала в агрегаты, а в складской разбивке возникало ведро `DATA_MISSING (назнач. DATA_MISSING)` с реальными количествами | разбивка `byWh` заполняется только при `card !== null`; такие поставки считаются счётчиком `whUnknown`, их количества остаются в общих суммах. В журнал печатается, что сумма по складам меньше общей на `whUnknown` поставок — «это не потеря, это неполнота чтения» |
| §9.4 сохраняла старую формулировку «первичный реестр движения поставок» | заменено на «реестр **объектов** поставок; факт определяется по `statusID` и `factDate`» |
| §9.5: «по 66 поставок» неточно — `4a` даёт 66, `4b` при 131 записи даёт 65 | заменено на «двумя диапазонами: первые 66 записей и все оставшиеся» |

Правило в шапке пробы переписано так, чтобы совпадать с кодом: исключается
ровно тот агрегат, который испорчен, а не все сразу.

---

## 10. Сверка с известными поставками, `probeJuly4()` 17.08.2026 13:22

### 10.1 Результат: шесть из шести

Проба вернула ровно **6 состоявшихся поставок** в окне 01–31.07.2026, и все
шесть совпали с тем, что владелец помнил поимённо, включая транзитные склады.

| Поставка | Факт | Назначение | Транзит / `actual` | Позиций | `quantity` | `accepted` | `ready` |
|---|---|---|---|---:|---:|---:|---:|
| 40321392 | 06.07 | Тула | Тула (транзита нет) | 3 | 40 | 40 | 40 |
| 41037541 | 24.07 | Волгоград | СК Обухово | 19 | 165 | 165 | 165 |
| 41039407 | 24.07 | Владимир Воршинское | СК Обухово | 19 | 165 | **164** | 164 |
| 41039644 | 24.07 | Сарапул | СК Обухово | 19 | 165 | 165 | 165 |
| 41047776 | 28.07 | Екатеринбург, Перспективная 14 | Чехов 2 | 19 | 170 | **169** | 169 |
| 41047801 | 28.07 | Новосемейкино (Самара) | Чехов 1 | 19 | 165 | 165 | **164** |

Итого 870 передано, 868 принято, 867 готово к продаже.

Одна запись отсеялась фильтром: `statusID 1`, создана 22.07, факта нет.
🔑 У неё `supplyID = null` — на этой стадии поставка идентифицируется только
`preorderID`.

🔴 **Отсюда НЕ следует «разный ключ на разных стадиях».** У завершённых поставок
есть оба идентификатора сразу — например `supplyID 40321392` при `preorderID
52279049`. Одна и та же сущность живёт сначала как `preorderID = X, supplyID =
null`, а позже как `preorderID = X, supplyID = Y`. Если менять первичный ключ по
стадии, эта поставка станет **двумя записями** в RAW.

Формулировка, которая держится на данных: `preorderID` — **кандидат** на
стабильный lifecycle-ключ, поскольку присутствует и до назначения `supplyID`, и
после; `supplyID` — второй идентификатор уже оформленной поставки. Уникальность
и отсутствие ветвления проверяются на всей истории функцией `p4KeyAudit_` в
`probeInventory4a/4b` — до проектирования первичного ключа.

### 10.2 🔴 Поправка к разделу 9: в июльской выборке `actualWarehouse` совпадает с транзитом

В разделе 9 поле названо «фактический склад». Июльская сверка это уточняет и
частично опровергает: на **пяти транзитных поставках июля** `actualWarehouseID`
**совпадает с `transitWarehouseID`** до идентификатора.

- 41037541: назначение Волгоград, `actual` = `transit` = СК Обухово (50141799).
- 41047776: назначение Екатеринбург, `actual` = `transit` = Чехов 2 (210001).
- 40321392: транзита нет, `transitWarehouseID = null`, `actual` = Тула = назначение.

То есть в этой выборке `actualWarehouse` ведёт себя как **точка физической сдачи
коробов**, а не как склад, где товар в итоге лёг. Конечная точка —
`warehouseName`. Читать `actual` как «где товар сейчас» на этих данных нельзя:
три поставки 24.07 «фактически» на Обухово, но Обухово — сортировочный центр, а
не место хранения.

⚠️ **Обобщение на всю историю пока не сделано.** Шесть поставок — это шесть
поставок, а не правило. Именно поэтому в `probeInventory4a/4b` добавлена
`p4WhRelation_()` со счётчиками `actual == transit` / `actual == назначение` /
прочее по всей выборке: правило либо подтвердится на 131 записи, либо нет.
До этого складская разбивка строится по `warehouseName` как по более
консервативному выбору — он не зависит от того, чем окажется `actual`.

### 10.3 Карточка поставки несёт больше, чем список

Помимо складов в карточке пришли: `transitWarehouseID` / `transitWarehouseName`,
`acceptanceCost`, `paidAcceptanceCoefficient`, `rejectReason`,
`supplierAssignName`, `storageCoef`, `deliveryCoef`, а также собственные
агрегаты `quantity`, `readyForSaleQuantity`, `acceptedQuantity`,
`unloadingQuantity`, **`depersonalizedQuantity`**, `canShowQuantity`.

Два поля стоит отметить отдельно:

- `storageCoef` / `deliveryCoef` по этим шести: Тула 160/160, Волгоград 120/125,
  Владимир 130/130, Сарапул 175/170, Самара 175/160, Екатеринбург 190/190. Это
  готовая фактура по экономике логистики в разрезе поставки.
- `depersonalizedQuantity` — «обезличенное количество», у всех шести равно нулю.
  Поле смыслово соседствует с обезличиванием складов; что именно оно считает,
  не установлено.

### 10.4 🔑 Отчёт о приёмке: пустое окно получило независимое объяснение

У всех шести июльских поставок `acceptanceCost = 0` и
`paidAcceptanceCoefficient = 0`. Это согласуется с тем, что отчёт о **платной**
приёмке за окно 18.07–17.08 вернул `204` без строк, тогда как сентябрь 2024 дал
строки с `total` 20 000 и 272.

**Основная гипотеза:** наличие строк в `acceptance_report` связано с платной
приёмкой. Два независимых источника указывают в одну сторону, но это по-прежнему
гипотеза, а не правило.

⚠️ Не проверено обратное — попадают ли в отчёт строки с нулевой стоимостью
вообще. Проверка: взять один период с `acceptanceCost > 0` и один с нулевой
стоимостью и посмотреть, что отчёт вернёт на каждом.

Практическое следствие до этой проверки: `acceptance_report` используется
**только как частичный контрольный источник там, где он фактически возвращает
строки**. Утверждать, что он покрывает ровно периоды платной приёмки, рано.

### 10.5 🔴 Расхождения внутри поставок — по SKU, а не по итогу

Три находки, каждая меняет требования к будущему балансу.

**Пересорт при сходящемся итоге.** Поставка 41039644 (Сарапул): итог 165 = 165,
но `Тоник УВЛ` 10 → **9**, а `Тоник Акне` 10 → **11**. Сумма сошлась, состав —
нет.
🔴 Значит баланс нельзя строить на итогах поставки: по SKU он разъедется, а
итоговая цифра этого не покажет.

**`accepted > quantity` встречается.** Тот же `Тоник Акне`: принято больше, чем
передано. Классификация в `probeInventory4` не зря содержит корзину «перебор» —
она не гипотетическая.

**`accepted` и `ready` — разные величины.** Поставка 41047801 (Самара): принято
165 из 165, но готово к продаже 164, а `unloadingQuantity` = 1 у
`Набор_4единицы_прыщ`. `rejectReason` при этом `null`.
Это та самая поставка, по которой WB выставил штраф за упаковку 6 320 ₽
(см. `wb-packaging-fine-dispute`). Совпадение отмечено; причинная связь **не
утверждается**.

Недостачи по позициям: `Сыворотка Акне` 20 → 19 (Владимир),
`Набор вишня+амбра` 5 → 4 (Екатеринбург).

### 10.6 Что это меняет в плане

1. Память владельца подтверждена документально — сверка как метод работает.
2. 🔴 **Грейн будущего реестра — `поставка × SKU`**, а не строка на поставку.
   Пересорт в Сарапуле на агрегате исчезает полностью: 165 = 165. Одна строка на
   поставку сделала бы такие расхождения ненаблюдаемыми в принципе.
3. В RAW нужны количества раздельно: `quantity`, `acceptedQuantity`,
   `readyForSaleQuantity`, `unloadingQuantity` и `depersonalizedQuantity` — они
   не равны друг другу и не выводятся одно из другого.
4. Ключ: `preorderID` — кандидат в lifecycle-ключ, `supplyID` — второй
   идентификатор оформленной поставки. Разные ключи по стадиям **не вводить**.
5. Склад хранить тремя полями: назначения, транзита и `actual`. Агрегировать по
   назначению; `actual` без сопоставления с транзитом не интерпретировать.

Следующий шаг: `probeInventory4b()` и `probeInventory4c()` — оставшаяся часть
истории. По их результатам подтверждается или опровергается предварительная
граница `acceptedQuantity`, поведение `actualWarehouse` и выводы по
идентификаторам. Загрузчик после них.

### 10.7 Ревю аудитора по разделу 10

| Замечание | Что сделано |
|---|---|
| 🔴 Код пробы сохранял старую трактовку: заголовок «склад назнач → фактический», а `byWh` агрегировался по `actualWarehouseName` — три поставки 24.07 легли бы на транзитное Обухово | ведро строится по `warehouseName`; `transit` читается отдельным полем, а не реконструируется из `actual`; в журнале `actual` печатается без интерпретации, с признаком `actual == transit` / `== назначение` / прочее и сводными счётчиками по всей выборке |
| 🔴 «`supplyID` для состоявшихся, `preorderID` для незавершённых» — при смене PK по стадии одна поставка станет двумя записями | переписано: `preorderID` — кандидат в lifecycle-ключ, проверяется функцией `p4KeyAudit_` (уникальность, пропуски, «один `preorderID` → несколько `supplyID`») по всей выборке без запросов к API |
| §10.4 утверждала как следствие то, что рядом названо непроверенным | понижено до основной гипотезы; названа конкретная проверка — период с `acceptanceCost > 0` против периода с нулевой стоимостью |

Аудит идентификаторов честно оговаривает свою границу: это проверка на текущем
снимке, переприсвоение идентификаторов во времени она поймать не может — для
этого нужны повторные снимки.

### 10.8 Третье ревю: запрос к `/supplies/null`

🔴 После находки «`supplyID = null` на стадии preorder» цикл инвентаризации всё
равно ходил в API безусловно — то есть на такой записи выполнял
`GET /api/v1/supplies/null` и `GET /api/v1/supplies/null/goods`. Отказ по
заведомо неверному адресу засчитывался в `cardErrors` и `goodsErrors` и пометкой
`DATA_MISSING`.

Это не ошибка чтения источника: у объекта просто ещё нет идентификатора, которым
эти методы адресуются. Счётчики, которыми мы собирались судить о здоровье
источника, оказались бы загрязнены гарантированным мусором — по одной ложной
ошибке на каждую незавершённую поставку.

Исправлено: перед запросами стоит guard `hasSupplyId`. Записи без `supplyID`
считаются отдельным счётчиком `noSupplyId`, помечаются в журнале
`NO_SUPPLY_ID: стадия preorder, карточка и состав не запрашивались`, и в
`cardErrors` / `goodsErrors` не попадают. Сводка это проговаривает явно:
«ошибки чтения — только по записям, где `supplyID` был».

Заодно §10.2 приведена к тому, что проба и должна проверять: заголовок и текст
говорят о **июльской выборке**, а обобщение на всю историю названо задачей
`p4WhRelation_()` в `probeInventory4a/4b`.

---

## 11. Инвентаризация, `probeInventory4a()` 17.08.2026 14:40 — записи 0…65

Обработано 66 записей из 131 (по `createDate`, сентябрь 2024 — декабрь 2025).
В агрегаты вошло 60 состоявшихся. Исключено 6 записей: у **всех шести**
отсутствует `factDate`, и одна из этих же шести дополнительно имеет
`statusID 3`. Категории пересекаются, а не складываются — журнал печатает их
как `(без factDate: 6, статус ≠ 5: 1)`. Ошибок чтения нет, расхождений
«карточка ↔ позиции» нет.

### 11.1 🔑 Предварительная граница `acceptedQuantity`: 20.08.2025

| Класс | Поставок |
|---|---:|
| `accepted = 0` | 38 |
| `0 < accepted < quantity` (недобор) | 9 |
| `accepted = quantity` | 11 |
| `accepted > quantity` (аномалия) | 2 |

- Последняя поставка с `accepted = 0`: **06.08.2025**.
- Первая поставка с `accepted > 0`: **20.08.2025**.
- Нулей после 20.08.2025 **внутри диапазона 0…65** — нет.

🔴 **Что именно доказано.** Только это: в записях 0…65 последняя нулевая
состоявшаяся поставка — 06.08.2025, первая положительная — 20.08.2025, и внутри
этого диапазона нулей после неё нет. Что в записях 66…130 не встретится более
поздняя состоявшаяся поставка с `accepted = 0`, **пока не проверено**. Июль 2026
показывает нормальные значения, так что вероятность высока — но вся эта работа
и держится на том, чтобы не превращать сильную гипотезу в факт до контрольного
прогона.

🔴 **Что это значит для баланса.** Итог по обработанным записям: `quantity`
35 919 против `accepted` 12 388. Разница ~23 500 единиц — это **не недостача**,
а поставки, у которых поле не заполнено. Посчитать их как утраченные означало бы
изготовить потерю в двадцать раз крупнее реальной.

Косвенная проверка: товар этих поставок продавался — продажи с сентября 2024
лежат в BigQuery. Если бы `accepted = 0` означало «не принято», продавать было бы
нечего. Значит наиболее вероятное объяснение — **поле начали заполнять**, а не
товар перестали принимать. ⚠️ Это по-прежнему реконструкция: WB нигде не
объявлял дату, и прямого подтверждения у нас нет.

**Предварительный рабочий порог — 20.08.2025.** Глобальным правилом он
становится только если `probeInventory4b/4c` не обнаружат более поздних
состоявшихся поставок с `accepted = 0`. Если не обнаружат — фиксируем
20.08.2025 как историческую границу источника; если обнаружат — граница не
временная, и объяснять поведение поля придётся иначе.

При подтверждении практический вывод такой: материальный баланс по приёмке
строится только с 20.08.2025, а за сентябрь 2024 — август 2025 из этого
источника доступно лишь `quantity` — сколько отправлено.

### 11.2 `actual` в диапазоне 0…65: третьего значения не встретилось

| Отношение | Записей |
|---|---:|
| `actual == назначение` | 50 |
| `actual == transit` | 7 |
| `actual` пуст | 9 |

🔴 **Что показывают эти счётчики, а что нет.** Каждый непустой `actual` из
диапазона 0…65 совпал либо со складом назначения (50), либо с транзитом (7);
значения, не равного ни тому ни другому, не встретилось. Девять `actual` пусты.

Строгого условия «транзит есть → `actual == transit`, транзита нет →
`actual == назначение`» отсюда **не следует**: `p4WhRelation_()` считает только
отношение `actual` к двум идентификаторам и не строит таблицу «есть транзит /
нет транзита». По строкам журнала первой половины это выглядит именно так, но
как правило подтверждается или опровергается после `4b` и `4c`.

🆕 Новое: **`actual` пуст у 9 записей**. В июльской выборке такого не было.
Девятка закрывается полностью: **шесть** — это записи без `factDate` (те самые
исключённые), ещё **три** — состоявшиеся записи с `factDate` и `statusID 5`, но
с пустым `preorderID` и `quantity` 1, 1 и 8 (позиции 58, 59 и 65). Что это за
служебные поставки, не установлено.

🔑 Транзитная схема появляется только с **30.10.2025** (поставка 33713549,
Екатеринбург через Чехов 2). Всё, что раньше, — прямые поставки на склад.

### 11.3 🔴 Ни `preorderID`, ни `supplyID` не покрывают всю выборку

Аудит идентификаторов по всем 131 записям:

| | Уникальных | Пустых | Дубликатов |
|---|---:|---:|---:|
| `preorderID` | 117 | **14** | 0 |
| `supplyID` | 115 | **16** | 0 |

Случаев «один `preorderID` → несколько `supplyID`»: **0**.

Это меняет вывод раздела 10. Пустые есть **у обоих** полей, причём в диапазоне
0…65 счётчик `noSupplyId` показал ноль — значит все 16 записей без `supplyID`
находятся во второй половине выборки, среди свежих заявок. А записи без
`preorderID` встречаются как раз в первой половине.

**Следовательно:** ни `supplyID`, ни `preorderID` по отдельности не покрывают
полный жизненный цикл поставки. Дубликатов и ветвлений нет — там, где оба поля
заполнены, связь один-к-одному.

🔴 **Первичный ключ пока не выбираем.** И `COALESCE(supplyID, preorderID)` в
голом виде в кандидаты не записываем: аудит доказал уникальность каждого поля
внутри своей колонки, но не уникальность объединённого пространства значений.
`supplyID = 123` одной записи и `preorderID = 123` другой дали бы коллизию.
Если такой fallback вообще понадобится, он должен нести namespace — `S:<id>` /
`P:<id>` — либо надо хранить оба поля и суррогатный ключ поверх.

Окончательный PK проектируется после анализа всех 131 записи.

### 11.4 Склады назначения по первой половине истории

| Склад | Поставок | Через транзит | `quantity` | `accepted` |
|---|---:|---:|---:|---:|
| Коледино | 11 | 0 | 8 652 | 2 602 |
| Казань | 12 | 1 | 8 002 | 1 038 |
| Краснодар (Тихорецкая) | 10 | 2 | 5 571 | 1 475 |
| Тула | 8 | 0 | 4 692 | 3 174 |
| Электросталь | 10 | 0 | 3 982 | 2 203 |
| Невинномысск | 6 | 1 | 3 391 | 270 |
| Екатеринбург — Перспективная 14 | 2 | 2 | 1 473 | 1 470 |
| Новосемейкино | 1 | 1 | 156 | 156 |

⚠️ Колонка `accepted` здесь **не** говорит о недостаче: она пуста у всего, что
раньше 20.08.2025. Сравнивать `quantity` и `accepted` по этой таблице нельзя.

### 11.5 Эксплуатационное: упор в лимит `supplies-api`

При паузе 350 мс `429` прилетал на каждой второй-третьей записи. Ретрай вытянул
все случаи, итог — ноль ошибок, но это запас удачи, а не конструкции. Прогон
занял 249 с при бюджете 270 с, то есть почти упёрся в лимит Apps Script.

Правки в пробе:

- пауза 350 → **900 мс**;
- до **трёх попыток** на запрос (первая + до двух повторов), задержка перед
  повтором растёт 4 с → 8 с;
- добавлен счётчик `P4_RATE_HITS_` — упоры в лимит видны в сводке;
- остаток выборки разбит на два прогона: `probeInventory4b()` — записи 66…99,
  `probeInventory4c()` — записи 100 и далее.

Для будущего загрузчика это ограничение существенно: 131 поставка × 2 запроса
при таком лимите — это не «дёрнуть за раз», а несколько минут работы с паузами.

### 11.6 Что осталось до проектирования загрузчика

1. Прогнать `probeInventory4b()` и `probeInventory4c()` — вторая половина
   истории, включая все 16 записей без `supplyID`. Они закрывают сразу три
   предварительных вывода: граница 20.08.2025, поведение `actual` и выбор
   первичного ключа.
2. Понять, что за записи с `quantity` 1 и пустым `preorderID`.
3. Проверить гипотезу о `acceptance_report` (§10.4) на периоде с
   `acceptanceCost > 0` — до 20.08.2025 это единственный источник данных о
   приёмке, и он становится важнее, чем казался.

### 11.7 Ревю аудитора по разделу 11

| Замечание | Что сделано |
|---|---|
| 🔴 «Граница найдена, и она чистая» / «баланс строится только с 20.08.2025» — доказано лишь для записей 0…65, а не для всей выборки | заголовок и текст переведены в предварительные; названо явно, что именно доказано и что нет; порог становится правилом только если `4b/4c` не найдут более поздних нулей |
| Вывод по `actual` сильнее данных: из трёх счётчиков не следует условие «есть транзит → `actual == transit`» | переписано на то, что счётчики действительно показывают — каждый непустой `actual` совпал либо с назначением, либо с транзитом, третьего значения не было; оговорено, что `p4WhRelation_()` не строит cross-tab «есть транзит / нет транзита» |
| `COALESCE(supplyID, preorderID)` записан в кандидаты без проверки уникальности объединённого пространства | убран из кандидатов: аудит доказал уникальность внутри каждой колонки, но не между ними — `supplyID = 123` и `preorderID = 123` дали бы коллизию. Если fallback понадобится, он должен нести namespace `S:<id>` / `P:<id>`. PK не выбирается до анализа всех 131 записи |
| `P4_RATE_HITS_` зависит от состояния execution context | сбрасывается в начале `p4Inventory_()` и `probeJuly4()` — контракт функции самодостаточен |
| Комментарий над `p4Fetch_()` говорил «с одним ретраем» при `P4_RETRIES_ = 3` | приведён в соответствие |

### 11.8 Второе ревю по разделу 11

| Замечание | Что сделано |
|---|---|
| 🔴 «исключено 6, из них 5 без `factDate` и одна со `statusID 3`» — категории сложены, хотя они пересекаются | исправлено: у всех шести нет `factDate`, одна из этих же шести дополнительно `statusID 3`. Журнал печатает `(без factDate: 6, статус ≠ 5: 1)` именно как пересекающиеся счётчики |
| Та же ошибка протекла в §11.2: «пять без `factDate` + три» давало 8 из 9 | исправлено на 6 + 3 = 9. Ошибка была не косметической: она создавала несуществующую десятую категорию ровно там, где мы исследуем странные служебные записи |
| `P4_RATE_HITS_` считает и `429`, и `5xx`, а последний отказ после исчерпания попыток в него не попадает | подпись в журнале приведена к тому, что счётчик реально считает. Разводить метрики `rate429` / `server5xx` — задача production-загрузчика, пробу ради этого не задерживаем |

### 11.9 Третье ревю по разделу 11

| Замечание | Что сделано |
|---|---|
| `P4_RETRIES_ = 3` при цикле `attempt < P4_RETRIES_` означает три ПОПЫТКИ, то есть два повтора — а комментарий и документ говорили «три ретрая» | константа переименована в `P4_MAX_ATTEMPTS_`, комментарии и §11.5 приведены к «до трёх попыток: первая плюс до двух повторов». Логика не менялась |
| §9.5 описывал пробу в настоящем времени как «три функции» и инвентаризацию «двумя диапазонами», хотя функций четыре и диапазонов три | актуализирован, с пометкой почему диапазонов стало три — ссылка на §11.5 |

### 11.10 Четвёртое ревю: остатки устаревших ссылок

| Замечание | Что сделано |
|---|---|
| §10.6 звала «следующий шаг прежний: `4a` и `4b`», хотя `4a` отработала, а диапазонов стало три | заменено на `4b` и `4c` с перечислением того, что именно они закрывают: границу `acceptedQuantity`, поведение `actualWarehouse` и выводы по идентификаторам |
| §9.5 после перечисления четырёх функций сохранял «Обе функции только читают» | заменено на «Все функции только читают и печатают в Execution log» |

Обе строки — след от расширения пробы с двух функций до четырёх. Полезное
наблюдение на будущее: при изменении состава функций проверять весь документ на
числительные («обе», «три функции», «двумя диапазонами»), а не только тот
раздел, который правишь.

---

## 12. Инвентаризация завершена: все 131 запись

`probeInventory4b()` 17.08.2026 15:12 — записи 66…99.
`probeInventory4c()` 17.08.2026 15:15 — записи 100…130.

Контрольные суммы по трём прогонам сходятся с заголовком списка: обработано
**131** запись, состоявшихся **108**, исключено **23**, без `supplyID` **16**.
Ошибок чтения — ноль во всех трёх прогонах.

### 12.1 🔴 Предварительная граница 20.08.2025 ОПРОВЕРГНУТА

Сводная классификация по всем 108 состоявшимся поставкам:

| Класс | Поставок |
|---|---:|
| `accepted = 0` | **40** |
| `0 < accepted < quantity` (недобор) | 17 |
| `accepted = quantity` | 48 |
| `accepted > quantity` (аномалия) | 3 |

После 20.08.2025 обнаружены **две состоявшиеся поставки с `accepted = 0`**:

| Поставка | Факт | Назначение | Позиций | `quantity` | `accepted` |
|---|---|---|---:|---:|---:|
| 36836448 | 16.02.2026 | Коледино | 1 | 77 | **0** |
| 37022425 | 20.02.2026 | Коледино | 1 | 76 | **0** |

🔴 **Чистой временной границы нет.** Гипотеза «поле начали заполнять с
20.08.2025» в жёсткой форме не проходит: два исключения лежат на полгода позже.
Хорошо, что порог держали предварительным — иначе правило «до 20.08.2025 нули не
считаем, после считаем» списало бы эти 153 единицы в недостачу автоматически.

Что общего у двух исключений — обе на Коледино, обе по одной позиции, объёмы 76
и 77 единиц. Причина **не назначается**: это может быть и незаполненное поле, и
настоящий ноль приёмки. Различить можно только по продажам — если товар этих
поставок в продажах не появился, ноль настоящий.

**Правило для загрузчика меняется.** Вместо порога по дате: `accepted = 0` при
`quantity > 0` хранить как **неизвестность с флагом**, а не как ноль. Дата
остаётся сильным признаком (38 из 40 нулей — до 20.08.2025), но не критерием.

### 12.2 🔴 Источник противоречит сам себе: два расхождения карточка ↔ позиции

Детектор `MISMATCH`, добавленный по требованию аудитора, сработал дважды:

| Поставка | Факт | Позиции: `accepted` | Позиции: `ready` | Карточка: `acceptedQuantity` |
|---|---|---:|---:|---:|
| 38229326 | 07.04.2026 | 138 | 58 | **58** |
| 39024769 | 02.05.2026 | 167 | 159 | **159** |

🔑 В обоих случаях `acceptedQuantity` карточки равен сумме
`readyForSaleQuantity` по позициям, а не сумме `acceptedQuantity`.

⚠️ Но это **не** общее правило: у июльской поставки 41047801 позиции дали
`accepted` 165 и `ready` 164, а карточка честно показала `acceptedQuantity` 165
и `readyForSaleQuantity` 164. То есть агрегат карточки совпадает с позициями в
106 случаях из 108 и расходится в двух.

**Следствие:** агрегаты карточки в загрузчик брать нельзя. Единственный источник
количеств — **позиции**, а карточка годится только для складов, дат и
коэффициентов. Расхождение по этим двум поставкам вынести в отдельную проверку.

### 12.3 `actual`: правило подтверждено на всей выборке

| Отношение | Карточек |
|---|---:|
| `actual == назначение` | 72 |
| `actual == transit` | 27 |
| `actual` пуст | 16 |

Прочитано 115 карточек (все записи с непустым `supplyID`). Значения, не равного
ни назначению, ни транзиту, **не встретилось ни разу за всю историю**.

Транзитных поставок 27 из 108, и все они появляются после 30.10.2025.

### 12.4 Материальный контур по складам назначения, вся история

| Склад назначения | Поставок | Через транзит | `quantity` | `accepted` |
|---|---:|---:|---:|---:|
| Казань | 17 | 5 | 10 072 | 3 077 |
| Коледино | 19 | 0 | 9 762 | 3 559 |
| Краснодар (Тихорецкая) | 17 | 8 | 6 616 | 2 438 |
| Электросталь | 18 | 0 | 5 987 | 4 202 |
| Тула | 18 | 0 | 5 549 | 3 994 |
| Невинномысск | 6 | 1 | 3 391 | 270 |
| Екатеринбург — Перспективная 14 | 7 | 7 | 2 475 | 2 473 |
| Новосемейкино | 2 | 2 | 321 | 321 |
| Волгоград | 1 | 1 | 165 | 165 |
| Владимир Воршинское | 1 | 1 | 165 | 164 |
| Сарапул | 1 | 1 | 165 | 165 |
| Склад Шушары | 1 | 1 | 120 | 120 |
| **Итого** | **108** | **27** | **44 788** | **20 948** |

`readyForSaleQuantity` по всем состоявшимся: 20 854.

🔴 **Колонку `accepted` в этой таблице нельзя читать как «принято».** В ней
сидят 40 нулей, из которых как минимум 38 — незаполненное поле. Разница
44 788 − 20 948 = 23 840 единиц **не является недостачей**.

⚠️ Склад назначения — точка поставки, а не текущее расположение остатка. С
обезличенным остатком не смешивать.

### 12.5 Ключи: картина закрыта

Аудит идентификаторов одинаков во всех трёх прогонах (он считает по всей
выборке): `preorderID` 117 уникальных при 14 пустых, `supplyID` 115 уникальных
при 16 пустых, дубликатов нет, ветвлений нет.

Теперь видно распределение пропусков:

- **16 записей без `supplyID`** — все в `statusID 1`, все без `factDate`; 2 в
  диапазоне 66…99 и 14 в 100…130. Это свежие заявки стадии preorder.
- **14 записей без `preorderID`** — все в первой половине истории, у всех есть
  `supplyID` и почти у всех `factDate`; объёмы 1, 2, 3, 8 единиц.

То есть пропуски у двух полей лежат на **разных концах** истории и по разным
причинам: справа — поставка ещё не оформлена, слева — старые служебные записи.

Вывод по ключу прежний и теперь окончательный: единственным первичным ключом не
годится ни одно поле; голый `COALESCE` небезопасен без namespace. Конструкцию
выбираем при проектировании RAW.

### 12.6 Эксплуатационное

`4b` — 113 с на 34 записи, 8 повторов после 429. `4c` — 55 с на 31 запись
(из них 14 без обращения к API), 3 повтора. Пауза 900 мс и три попытки
отработали: ошибок чтения ноль в обоих прогонах.

Правка в пробе после этих прогонов: вердикт о границе теперь явно помечен как
**локальный для диапазона**. В `4c` он напечатал «нулей после 2026-05-02 нет» —
и это правда для записей 100…130, но глобально граница уже сломана двумя
февральскими поставками из `4b`. Формулировка исправлена, чтобы такой вердикт
нельзя было прочитать как ответ по всей выборке.

### 12.7 Что осталось до проектирования загрузчика

1. Проверить по продажам две февральские поставки на Коледино с `accepted = 0`:
   появился ли этот товар в продажах. Это различит «поле не заполнено» и
   «товар не принят».
2. Разобрать два расхождения карточка ↔ позиции (38229326 и 39024769).
3. Проверить гипотезу §10.4 об `acceptance_report` на периоде с
   `acceptanceCost > 0`.
4. Понять природу 14 старых записей без `preorderID` с объёмами 1–8 единиц.

---

## 13. Ветка нулей закрыта первичными документами WB

По двум февральским поставкам, сломавшим временную границу, владелец поднял
сверочные отчёты и акты приёмки WB.

| Поставка | Дата факта | `quantity` | `acceptedQuantity` API | Документально принято | Что произошло |
|---|---|---:|---:|---:|---|
| 36836448 | 16.02.2026 | 77 | 0 | **77** | принято как «неопознанный товар» |
| 37022425 | 20.02.2026 | 76 | 0 | **76** | принято как «неопознанный товар» |
| **Итого** | | **153** | **0** | **153** | физическая приёмка состоялась |

В сверочном отчёте по строке SKU стоит «Принято складом» 0, а отдельной строкой
«Неопознанные товары» — те же 77 (и 76) штук принятыми. Акты приёмки №36836448 и
№37022425 фиксируют это же количество фактически принятым.

Причина операции — товар не был сопоставлен с заявленным SKU при приёмке —
известна владельцу по обстоятельствам конкретной отгрузки; сами файлы её текстом
не называют. Документы независимо подтверждают **результат**: товар был на
складе и принят физически, но отнесён в «неопознанные».

### 13.1 🔑 Что именно доказано про `acceptedQuantity = 0`

**`acceptedQuantity = 0` не доказывает физического непринятия товара.**

В двух документально проверенных поставках — `36836448` и `37022425` — товар был
физически принят как **неопознанный**: по строке SKU ноль, отдельной строкой
«Неопознанные товары» полное количество.

🔴 **Это доказано для этих двух поставок, а не для всех сорока нулей.** У ранних
поставок сентября 2024 — августа 2025 держится другой сильный признак: поле
массово равно нулю, хотя товар очевидно участвовал в обороте. Это может быть
и legacy-незаполненность, и то же самое, и что-то третье. **Конкретная причина
остальных нулей не установлена — и для нашей задачи не требуется.**

Три следствия:

1. Расследование остальных нулей **не нужно не потому, что механизм понятен, а
   потому что `acceptedQuantity` не участвует в расчёте**. Вопрос вынесен из
   критического пути, а не закрыт.
2. `acceptedQuantity` **не используется** для расчёта исторического ввоза
   товара на WB. Он остаётся диагностическим полем.
3. Историческим потоком «передано в поставку WB» служит
   `supplies/{supplyID}/goods.quantity` на грейне поставка × SKU. Июльская
   сверка 6/6 и эти два акта показывают, что `quantity` соответствует реально
   отправленному объёму.

### 13.2 Расследование источника завершено

Все вопросы, ради которых заводились пробы №1–№4, закрыты:

| Вопрос | Ответ |
|---|---|
| Где брать историю поставок | `supplies-api`, три метода, разные глаголы |
| Какая глубина | вся история с 03.09.2024, 131 запись, без усечения |
| Что считать состоявшейся поставкой | `statusID 5` и непустой `factDate` — 108 из 131 |
| Какой показатель брать за «привезено» | `goods.quantity` на грейне поставка × SKU |
| Что значит `accepted = 0` | физического непринятия НЕ доказывает; причина установлена только для двух поставок, §13.1 |
| Что такое `actual` | точка сдачи: транзит, если он есть, иначе назначение |
| Можно ли верить агрегатам карточки | нет, §12.2 — брать позиции |
| Как ключевать | рабочий lifecycle-key `P:<preorderID>`, fallback `S:<supplyID>`; стабильность связи `preorderID → supplyID` во времени **не считается доказанной** и контролируется инвариантом И7 |

Следующий шаг — не проба, а проектирование: `docs/wb_supplies_raw_design.md`.
