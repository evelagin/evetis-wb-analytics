# Stage 4A · PR2 — протокол выката и приёмки на production

**Дата выката:** 21.08.2026, 05:35–05:43 UTC (08:35–08:43 МСК)
**Проект:** `project-fa311fc0-4d87-4781-986`, датасет `wb_mart`
**Основание:** ACK владельца на rollout PR2.
**Итог: 18 гейтов, PASS 18, FAIL 0. K9 PASS ×3 побайтно. Рекомендация — `APPROVE`.**

---

## 0. 🔴 Якорь сместился между валидацией и выкатом — и это правильно

Валидация PR2 прошла вечером **20.08**, выкат — утром **21.08**. Между ними отработала
ночная сборка витрины. Все абсолютные числа сдвинулись:

| величина | 20.08 (валидация) | 21.08 (выкат) |
|---|---|---|
| `MART_SKU_DAILY` строк | 7 302 | **7 327** |
| `SUM(ad_spend)` | 523 365,38 ₽ | **523 367,06 ₽** |
| `V_DASH_COVERAGE_DAILY` строк | 715 | **716** |
| `V_ADS_SCREEN_QUERY` строк | 1 821 | **1 815** |
| сбор маркетплейса | 7 954 656,56 ₽ | **7 960 491,08 ₽** |
| к перечислению | 18 870 292,80 ₽ | **18 877 313,36 ₽** |
| контрибуция за покрытое окно | 1 312 034,95 ₽ / 128 сут | **1 317 656,29 ₽ / 129 сут** |
| витрина без гейта | −7 777 098,96 ₽ | **−7 768 266,13 ₽** |
| тождество D6 | 9 660 106,53 ₽ | **9 664 117,06 ₽** |
| provisional-суток в контрибуции | 2 | **3** |
| контрактная свежесть `KPI_DAILY` | 18.08 | **19.08** |

**Ни один гейт от этого не упал** — они сформулированы как соотношения, а не как
замороженные константы (урок №2 выката 16.08). Требовать совпадения с числом
вчерашнего дня означало бы требовать, чтобы данные не обновлялись.

⚠️ Все числа ниже — замеры **21.08.2026**.

---

## 1. Что выкачено

| объект | тип | грейн | строк | `view_definition` |
|---|---|---|---|---|
| `wb_mart.V_DASH_KPI_DAILY` | VIEW | `day` | **716** | 14 398 симв. |
| `wb_mart.V_DASH_SKU_DAILY` | VIEW | `day × nm_id` | **7 327** | 4 108 симв. |
| `wb_mart.V_DASH_FRESHNESS_BY_CONTRACT` | VIEW | `contract_code` | **2** | 2 662 симв. |

Три `CREATE OR REPLACE VIEW`, выполнены по одному. Других DDL/DML не выполнялось.

---

## 2. Результаты D1–D18 на физических объектах

| # | проверка | измерено 21.08 | вердикт |
|---|---|---|---|
| D1 | грейн KPI = календарь PR1 | 716 = 716, `DISTINCT day` = 716 | **PASS** |
| D2 | грейн SKU = витрина | 7 327 = 7 327, уникальных ключей 7 327 | **PASS** |
| D3 | fan-out справочника | `MAX(ref_rows) = 1`, сирот 0, без названия 0, SKU 25 | **PASS** |
| D4 | SKU-часть KPI = агрегат SKU-вью | заказы 3 348 781,12 = 3 348 781,12 · вклад 1 317 656,29 = 1 317 656,29 · и ещё четыре величины | **PASS** |
| D5 | сбор и выплата = `FACT_FINANCE` (matched) | 7 960 491,08 = 7 960 491,08 · 18 877 313,36 = 18 877 313,36 | **PASS** |
| D6 | 🔴 **сумма частей = целое** | 4 109 195,73 + 241 193,40 + 14 711,02 + 5 299 016,91 = **9 664 117,06**, остаток **0,00** | **PASS** |
| D7 | 🔴 **контрибуция наследует покрытие** | **+1 317 656,29 ₽** на 129 сутках; витрина без гейта **−7 768 266,13 ₽**; протечек 0 | **PASS** |
| D8 | NULL propagation | протечек KPI **0**, SKU **0** | **PASS** |
| D9 | нет `IFNULL(...,0)` / `COALESCE(...,0)` | KPI 0/0 · FRESHNESS 0/0 · SKU 1 `IFNULL`, и это разрешённое `ref_rows_for_nm_id`; `COALESCE(...,0)` — 0 везде | **PASS** |
| D10 | нет ratio-колонок | по `INFORMATION_SCHEMA.COLUMNS` — **0** совпадений | **PASS** |
| D11 | `AVG(ratio) ≠ ratio-of-sums` | **15,31 %** против **15,69 %** | **PASS** |
| D12 | PROVISIONAL наблюдаем | 14–20.08 → **3**, закрытый период 13.04–13.08 → **0**, противоречий 0 | **PASS** |
| D13 | signed settled + отдельная корректировка | вью **26 802 774,72** = signed; unsigned (MART-style) 26 816 654,72; смещение **13 880,00 = 2 × возвраты**; корректировка **1 834,88** | **PASS** |
| D14 | карта контрактов | `KPI_DAILY` 5 слоёв / 19.08 / OK · `SKU_DAILY` 6 слоёв / 19.08 / OK · опечаток 0 · `ref_sku_master` назван в списке «без даты» | **PASS** |
| D15 | contract-свежесть не подменяет системную | система **17.08 / STALE** ≤ контракт **19.08 / OK** | **PASS** |
| D16 | baseline «до = после» | все восемь величин совпали; см. §4 | **PASS** |
| D17 | **K9** | **PASS ×3 побайтно**, с первого прогона; см. §3 | **PASS** |
| D18 | Stage 3B.1 не задет | во всех трёх объектах `V_ADV_COSTS` 0 · `FACT_ADS_COSTS_DAILY` 0 · `V_ADS_SCREEN` 0 | **PASS** |

**18 PASS · FAIL 0.**

---

## 3. K9 — совпало с первого раза

Правило, выведенное в PR1 (делить файл **только** по `CREATE OR REPLACE VIEW`,
терминатор `;` снимать **после** нормализации), сработало: расхождений нет.

| объект | norm chars / lines | sha256 файла | sha256 BigQuery |
|---|---|---|---|
| `V_DASH_KPI_DAILY` | 10 472 / 172 | `361e690d…e44e7a` | `361e690d…e44e7a` ✅ |
| `V_DASH_SKU_DAILY` | 3 858 / 85 | `990c0305…53fbef` | `990c0305…53fbef` ✅ |
| `V_DASH_FRESHNESS_BY_CONTRACT` | 2 601 / 52 | `cb0d56ab…acb9b0b` | `cb0d56ab…acb9b0b` ✅ |

Правка комментария `CORRECTION-2` (числа 240 651,51 → 240 900,49 и 14 960,00 → 14 711,02),
внесённая перед выкатом, на хэши не повлияла — нормализация снимает строки-комментарии.
Числа в комментарии всё равно исправлены: комментарий, врущий о данных, хуже отсутствующего.

---

## 4. Baseline существующих объектов: до = после

| величина | до (05:35 UTC) | после (05:43 UTC) | delta |
|---|---|---|---|
| `MART_SKU_DAILY` строк | 7 327 | 7 327 | **0** |
| `SUM(ad_spend)` | 523 367,06 ₽ | 523 367,06 ₽ | **0,00** |
| `V_ADS_SCREEN_SKU` | 14 | 14 | **0** |
| `V_ADS_SCREEN_QUERY` | 1 815 | 1 815 | **0** |
| `FACT_ADS_COSTS_DAILY` строк / сумма | 1 816 / 514 064,00 ₽ | 1 816 / 514 064,00 ₽ | **0** |
| `V_DATA_FRESHNESS` | 13 | 13 | **0** |
| `V_DASH_COVERAGE_DAILY` (PR1) | 716 | 716 | **0** |
| `V_DASH_FRESHNESS_HEADER` (PR1) | 17.08 / STALE | 17.08 / STALE | **без изменений** |
| объектов `V_DASH*` | **2** | **5** | +3 — единственное изменение |

---

## 5. Reconciliation на живых объектах

```
orders_revenue_rub          3 348 781,12   KPI = SKU, delta 0,00
contribution_pre_cogs_rub   1 317 656,29   KPI = SKU, delta 0,00
marketplace_fee_rub         7 960 491,08   = FACT_FINANCE (matched), delta 0,00
net_settlement_rub         18 877 313,36   = FACT_FINANCE (matched), delta 0,00
sales_revenue_settled_rub  26 802 774,72   signed: Продажа + , Возврат −
                                           MART-style unsigned 26 816 654,72
                                           смещение 13 880,00 = 2 × возвраты
sales_adjustment_rub            1 834,88   «Коррекция продаж», отдельно, не выручка
```

**Разложение расходов замкнуто без остатка:**

```
4 109 195,73  SKU-уровень, который несёт витрина (logistics + wb_reward)
  241 193,40  SKU-уровень, которого в витрине нет (acquiring · loyalty · penalty · возмещения)
   14 711,02  SKU-уровень вне universe витрины
5 299 016,91  уровень счёта (storage · deduction · acceptance · penalty · возмещения · прочее)
─────────────
9 664 117,06  = SUM(V_WB_FINANCE_AMOUNTS_LONG_MAPPED)      остаток 0,00
```

Это гейт D6 и он же — доказательство, что `CORRECTION-2` закрыта: 241 193,40 ₽,
которые по первоначальному дизайну исчезли бы с экрана, теперь видимы отдельными
колонками. В контрибуцию они не входят (сидят внутри спреда) — и это подписано.

---

## 6. Свежесть по контракту против системной

| | значение | что задаёт |
|---|---|---|
| системный минимум (`V_DASH_FRESHNESS_HEADER`) | **17.08.2026 · STALE** | `ads_costs` — рекламный биллинг, на экран не выводится |
| контракт `KPI_DAILY` | **19.08.2026 · OK** | `finance` / `mart_sku_daily` — то, из чего собран экран |
| контракт `SKU_DAILY` | **19.08.2026 · OK** | то же + `ref_sku_master` (без даты, назван отдельно) |

Разрыв вырос до **двух суток**: биллинговый слой стареет, а KPI — нет. Ровно ситуация,
ради которой OBSERVATION-1 и вводилось. Оба значения живут одновременно, системный
минимум не тронут, карта «контракт → слои» видна в `view_definition` и проверена K9.

---

## 7. Отклонения от контракта PR2

**Одно, зафиксированное до выката и уже принятое вами: `CORRECTION-2`** —
дизайн §4 предполагал две группы расходов, в данных их четыре. Реализовано и закрыто
гейтом D6. Других отклонений нет: грейны, имена, правила NULL, закрытый список ratio
и границы scope — как в утверждённом контракте.

Наблюдение без действия: `ads_costs` остаётся `STALE` четвёртые сутки подряд
(`data_as_of = 17.08`). PR2 его не использует, на KPI это не влияет —
но системная шапка будет краснеть, пока Stage 3B.1 в Фазе A.

---

## 8. Состояние production и репозитория

**BigQuery**

* добавлено: `V_DASH_KPI_DAILY`, `V_DASH_SKU_DAILY`, `V_DASH_FRESHNESS_BY_CONTRACT` — только VIEW;
* изменено: **ничего** (D16);
* удалено: ничего;
* объектов `V_DASH*` теперь **5**: два из PR1 + три из PR2;
* Stage 3B.1 Фаза A не тронута, bootstrap не запускался.

**Репозиторий**

`sql/dash/dashboard_contract_v2.sql` · `sql/dash/dashboard_contract_v2_validation.sql` ·
`docs/STAGE4A_PR2_DESIGN_2026-08-20.md` · `docs/STAGE4A_PR2_IMPLEMENTATION_2026-08-20.md` ·
`docs/STAGE4A_PR2_ROLLOUT_2026-08-21.md` (этот протокол).

🔴 **Ветка, коммит и PR не созданы** — ваша половина процесса.

**Откат** — полон, без побочных эффектов:

```sql
DROP VIEW `wb_mart.V_DASH_KPI_DAILY`;
DROP VIEW `wb_mart.V_DASH_SKU_DAILY`;
DROP VIEW `wb_mart.V_DASH_FRESHNESS_BY_CONTRACT`;
```

Объекты PR1 и все существующие витрины не изменялись.

---

## 9. Рекомендация

# `APPROVE`

18 гейтов, FAIL 0, K9 побайтно с первого прогона, production не пострадал.

**Слой данных под первый экран готов полностью.** `Executive · Пульс магазина`
собирается в Looker Studio поверх четырёх готовых источников:
`V_DASH_KPI_DAILY` · `V_DASH_SKU_DAILY` · `V_DASH_FRESHNESS_BY_CONTRACT` ·
`V_DASH_FRESHNESS_HEADER` + `V_DATA_FRESHNESS`.
Data contract экрана — `docs/STAGE4A_PR2_IMPLEMENTATION_2026-08-20.md` §12.

**PR3 (когортный `buyout_rate`) не начат** и не будет начат без отдельного ACK.
