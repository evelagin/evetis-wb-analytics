# WB API Coverage Map — EVETIS WB Analytics

> Карта покрытия WB API для проекта **EVETIS WB Analytics**.
> Опирается на `docs/data_source_policy.md` (целевая модель API-first / API-only) и `docs/stabilization_plan.md` (§9 — первый приоритет: проверить покрытие WB API и построить API-only карту данных).
> Документ **не меняет код**, **не создаёт новых функций**, **не объявляет покрытие WB API подтверждённым** и не фиксирует контрольные значения как эталон.
> Назначение — инвентаризация: какие показатели *предположительно* доступны через WB API, на каком уровне детализации, как связывать с SKU/nmId/advertId и куда относить в P&L.
> Статус: проект на проверку. Все непроверенные позиции — `TBD / требует проверки`.

---

## 1. Цель документа

- Зафиксировать **карту покрытия WB API** по всем блокам данных, нужным для регулярного P&L и юнит-экономики.
- Для каждого блока определить *предполагаемый* источник в API, целевой RAW-лист, уровень детализации, ключи связи и место в P&L.
- Явно отделить то, что **берётся из WB API**, от того, что **по своей природе приходит из внутренних справочников** (себестоимость).
- Зафиксировать все пробелы и неопределённости как `TBD`, не закрывая их Excel/Drive в обход политики и не объявляя API полным до фактической проверки.

Документ — **инвентаризационный**. Он не утверждает, что переход на API-only выполнен, и не подтверждает ни один endpoint как рабочий до проверки (§9).

---

## 2. Связь с `docs/data_source_policy.md` и `docs/stabilization_plan.md`

- **`data_source_policy.md`** задаёт целевую архитектуру **API-first / API-only**: производственный источник регулярной аналитики — только WB API; Excel/Drive — сверка / исторический аудит / fallback. §8 этого документа перечисляет блоки, покрытие которых нужно проверить (статус по умолчанию — «требует проверки»). Настоящая карта — детализация этого §8.
- **`stabilization_plan.md`** (§9, шаги 1.1–1.5) делает «проверить покрытие WB API и построить API-only карту данных» **первым приоритетом стабилизации**. Настоящий документ закрывает шаги 1.1 (инвентаризация по блокам) и 1.2 (карта целевого пути) на уровне *описания*, без изменения кода.
- Карта **не переписывает** ни один из этих документов. Любые вытекающие правки вносятся отдельно, с записью в `CHANGELOG.md`.
- Принципы, которые карта наследует и не нарушает: один производственный источник; реклама по SKU — не факт без связки `date + nmId + advertId + spend`; неотнесённые расходы — только по утверждённому правилу; эталон подтверждается на API-only периоде, а не на Excel/Drive.

---

## 3. Целевая схема API-only потока
| Слой | Лист(ы) | Целевой источник | Примечание |
|---|---|---|---|
| RAW (регулярный поток) | `RAW_WB_ORDERS`, `RAW_WB_SALES_RETURNS`, `RAW_WB_FINANCE`, `RAW_WB_ADS`, `RAW_WB_STOCKS`, `RAW_WB_STORAGE` | WB API | Единственный производственный источник регулярной аналитики |
| RAW (сверка/история/fallback) | те же листы, помеченные `load_id` Excel/Drive | Excel/Drive | Только вне регулярного потока, с явной пометкой источника |
| CLEAN | `CLEAN_WB_DAILY` | производный от RAW | Источник наследуется от RAW; в целевой модели ручных формул поверх RAW быть не должно |
| ЮНИТ / P&L | `UNIT_SKU_DAILY`, `PNL_TOTAL` | производный от CLEAN/RAW | Факт берётся из API-данных в RAW; себестоимость — из справочников |
| DASHBOARD | `DASHBOARD_WB` и витрины | производный | Отображение, без собственного источника факта |

> Себестоимость в эту схему как «WB API → RAW» **не входит**: она поступает из внутренних справочников (§8 ниже) и подмешивается на уровне ЮНИТ / P&L.

---

## 4. Таблица покрытия WB API по блокам

Колонки для каждого блока: показатель · предполагаемый WB API endpoint / раздел · целевой RAW-лист · уровень детализации · ключи связи · куда в P&L · надёжная привязка к SKU? · статус.

> ⚠️ Колонка «предполагаемый WB API endpoint / раздел» содержит **гипотезы**, а не подтверждённые методы. Точный метод/версия/параметры по каждому блоку — `TBD` до фактической проверки в §9. Ни одна строка не считается подтверждённым покрытием.

| Блок | Показатель | Предполагаемый WB API раздел (TBD: точный метод) | Целевой RAW-лист | Уровень детализации | Ключи связи | Куда в P&L | Надёжно к SKU? | Статус |
|---|---|---|---|---|---|---|---|---|
| Заказы | Кол-во заказов, сумма заказов | WB Statistics API, раздел заказов (предполож.) | `RAW_WB_ORDERS` | дата / nmId / операция (srid) | `srid`, `wb_nm_id`, `barcode`, `wb_vendor_code` → `internal_sku` | Воронка / опер. показатель (не прямой P&L) | TBD | TBD / требует проверки |
| Продажи | Кол-во продаж, выручка реализации | WB Statistics API, раздел продаж (предполож.) | `RAW_WB_SALES_RETURNS` | дата / nmId / операция (srid) | `srid`, `wb_nm_id`, `barcode` → `internal_sku` | Выручка / реализация | TBD | TBD / требует проверки |
| Возвраты | Кол-во возвратов, сумма возвратов | WB Statistics API, раздел продаж/возвратов (предполож.) | `RAW_WB_SALES_RETURNS` (`is_return`) | дата / nmId / операция | `srid`, `wb_nm_id` → `internal_sku` | Вычет из выручки / возвраты | TBD | TBD / требует проверки |
| Остатки | Кол-во на складах (доступно/в пути/брак) | WB Statistics/Supplies API, раздел остатков (предполож.) | `RAW_WB_STOCKS` | snapshot-дата / nmId / склад | `wb_nm_id`, `barcode`, `warehouse_id` → `internal_sku` | Не P&L (оборачиваемость, план поставок) | TBD | TBD / требует проверки |
| Финансовый отчёт реализации | Реализация WB, к перечислению, комиссия, эквайринг | WB Statistics API, отчёт о реализации (предполож.) | `RAW_WB_FINANCE` | операция / дата / nmId | `rr_dt`/`sale_dt`, `wb_nm_id`, `srid` → `internal_sku` | Реализация, комиссия, к перечислению | TBD | TBD / требует проверки |
| Логистика | Услуги доставки покупателю (`logistics_amount`) | тот же отчёт реализации (предполож.) | `RAW_WB_FINANCE` | операция / дата / nmId | `rr_dt`, `wb_nm_id` → `internal_sku` | Расход: логистика WB | TBD (зависит от детализации отчёта) | TBD / требует проверки |
| Хранение | Плата за хранение (`storage_fee`) | отчёт реализации и/или отдельный отчёт хранения (предполож.) | `RAW_WB_FINANCE` (агрегат) и/или `RAW_WB_STORAGE` (по SKU) | дата / nmId (для `RAW_WB_STORAGE`); операция/период (для финотчёта) | `date`, `wb_nm_id` → `internal_sku` | Расход: хранение WB | TBD (по SKU — только если отдаёт `RAW_WB_STORAGE`-источник) | TBD / требует проверки |
| Удержания | Прочие удержания (`deduction`) | отчёт реализации, раздел удержаний (предполож.) | `RAW_WB_FINANCE` | операция / дата; nmId — не всегда | `date`, при наличии `wb_nm_id` | Расход: удержания WB (часть — кабинетные) | TBD (часть не привязывается к SKU) | TBD / требует проверки |
| Платная приёмка | Операции на приёмке (`acceptance`) | отчёт реализации, раздел приёмки (предполож.) | `RAW_WB_FINANCE` | операция / дата; nmId — не всегда | `date`, при наличии `wb_nm_id` | Расход: приёмка | TBD (часто кабинетный/партийный) | TBD / требует проверки |
| Реклама | Кампании, показы, клики, заказы, метрики | WB Advert/Promotion API, статистика кампаний (предполож.) | `RAW_WB_ADS` | дата / nmId / advertId | `advert_id`, `stat_date`, `wb_nm_id` → `internal_sku` | Аналитика рекламы (не прямой SKU-факт) | TBD (см. §6) | TBD / требует проверки |
| Рекламные расходы | `spend` по кампаниям + удержание «WB Продвижение» в финотчёте | WB Advert/Promotion API (`spend`) + отчёт реализации (удержание) (предполож.) | `RAW_WB_ADS` (`spend`) + `RAW_WB_FINANCE` (удержание) | дата / advertId / nmId (ADS); операция/дата (финотчёт) | `advert_id`, `stat_date`, `wb_nm_id`; сверка ADS ↔ удержание финотчёта | Расход: реклама (кабинет или оценка по SKU) | TBD (см. §6) | TBD / требует проверки |
| Детализация рекламы по date / nmId / advertId | Связка `date + nmId + advertId + spend` | WB Advert/Promotion API, детальная статистика (предполож.) | `RAW_WB_ADS` | дата / nmId / advertId | `stat_date` + `wb_nm_id` + `advert_id` + `spend` | Условие для перевода рекламы в SKU-факт (§6) | **Не подтверждено** — ключевой вопрос проверки | TBD / требует проверки |
| Себестоимость (внутренние справочники) | COGS на SKU/дату, состав наборов | **Не из WB API** | `SKU_MASTER`, `COST_HISTORY`, `BUNDLES` | SKU / nmId / дата (история) | `internal_sku` ↔ `wb_nm_id`; `BUNDLES`: набор → состав | Расход: себестоимость (COGS) | По справочнику (зависит от полноты `COST_HISTORY`/`BUNDLES`) | TBD / требует проверки полноты справочника (не API) |

> Сокращения: srid — идентификатор операции WB; nmId — `wb_nm_id`; advertId — `advert_id`. Привязка к SKU везде идёт через цепочку `wb_nm_id` / `barcode` / `wb_vendor_code` → `internal_sku` (`SKU_MASTER`).

---

## 5. Отдельный раздел по рекламе

- Реклама **не считается точным SKU-расходом**, пока фактически не подтверждена надёжная связка `date + nmId + advertId + spend` в данных WB API.
- До подтверждения этой связки рекламные расходы учитываются одним из способов:
  - **кабинетный расход** — общий расход на рекламу по кабинету (строка верхнего блока, например «WB Продвижение» / «Расход на рекламу»), без разнесения по SKU; либо
  - **распределяемая оценка** — разнесение по SKU только по **утверждённому правилу** и **только с явной пометкой «оценка»** (не «факт»).
- Целевая структура `RAW_WB_ADS` должна позволять хранить поля `advert_id`, `stat_date`, `wb_nm_id`, `spend`; фактическое наличие и корректность этой связки в реальных API-данных требует проверки (`TBD`).
- Обязательная сверка при проверке: `sum(RAW_WB_ADS.spend)` за период ↔ удержание «WB Продвижение» в `RAW_WB_FINANCE`. Расхождение (`delta`) фиксируется и классифицируется; до согласования `delta` реклама по SKU остаётся оценкой.
- Запрет: показывать рекламу по SKU как факт при недоказанной связке — нарушение правила из `CLAUDE.md` и `stabilization_plan.md`.

---

## 6. Отдельный раздел по неотнесённым расходам

Расход, который **нельзя надёжно отнести к конкретному SKU**, учитывается строго одним из утверждённых способов:

- **кабинетный расход** — относится на кабинет целиком, не разносится по SKU (примеры из текущей модели: «Возмещение ПВЗ», «Транзитные поставки WB», часть удержаний и приёмки);
- **отдельный блок** — выделяется в самостоятельную строку/блок отчёта без искажения SKU-экономики;
- **распределение по утверждённому правилу** — разносится по SKU **только** по заранее зафиксированному правилу и **только** с пометкой «оценка».

Запреты:
- **Запрещено ручное распределение без утверждённого правила.** Способ учёта каждого неотнесённого расхода фиксируется отдельным решением с записью в `CHANGELOG.md`.
- Запрещено выдавать оценку за факт. Любое распределение по правилу маркируется как «оценка».
- Запрещён двойной учёт: если расход уже учтён кабинетным блоком, он не разносится повторно по SKU.

---

## 7. Отдельный раздел по внутренним справочникам

Себестоимость и состав товаров **не приходят из WB API** — это нормально и соответствует модели API-first. Проверяется не наличие в API, а **полнота и корректность внутреннего справочника**.

| Справочник | Назначение | Ключи / связь | Что проверять |
|---|---|---|---|
| `SKU_MASTER` | Главный справочник товаров; маппинг `wb_nm_id` / `barcode` / `wb_vendor_code` → `internal_sku` | `internal_sku` ↔ `wb_nm_id` | Полнота активных SKU; однозначность маппинга; `sku_match_status` |
| `COST_HISTORY` | История себестоимости по SKU/дате | `internal_sku` + дата | Наличие COGS на нужные даты; отсутствие пропусков по активным SKU |
| `BUNDLES` | Состав наборов (комплект → компоненты) | набор `internal_sku` → состав | Корректная декомпозиция COGS набора по компонентам |

- COGS подмешивается на уровне **ЮНИТ / P&L**, а не в RAW из API.
- Контрольные суммы по `SKU_MASTER` и `COST_HISTORY` обязательны (по `stabilization_plan.md`).
- Себестоимость **не искать в WB API** ни при каких условиях.

---

## 8. Критерии готовности API-only блока

Блок считается готовым к работе в режиме API-only только при выполнении **всех** условий (по `data_source_policy.md` §9):

1. Блок доступен через WB API в нужной детализации (по дате и nmId/advertId, где применимо) — подтверждено фактически, а не предположительно.
2. Точный метод/версия/параметры API зафиксированы (не `TBD`).
3. Целевой RAW-лист и маппинг полей подтверждены; привязка к SKU через `internal_sku` работает (контроль `sku_match_status`).
4. Данные API за контрольный период сверены; расхождение API vs Excel/Drive разобрано и классифицировано (ошибка загрузки / разница методологии / округление / непокрытый блок).
5. Загрузка идемпотентна, без дублей (контроль через `IMPORT_LOG_*`, дедуп по `row_hash`).
6. Для рекламы — подтверждена связка `date + nmId + advertId + spend`, иначе блок остаётся «кабинет/оценка» (§5).
7. Способ учёта неотнесённой части зафиксирован по утверждённому правилу (§6).
8. Решение и контрольные значения записаны в `CHANGELOG.md`; соответствующий Excel/Drive-модуль помечен legacy.

До выполнения всех условий блок остаётся в смешанном режиме, Excel/Drive сохраняет роль сверки/fallback, а статус в карте — `TBD / требует проверки`.

---

## 9. Что не делать

| Не делать | Почему |
|---|---|
| Объявлять любой блок «покрыт API» до фактической проверки | Покрытие не подтверждено; статус по умолчанию — `TBD` |
| Указывать конкретный endpoint как рабочий без проверки | Точный метод/версия — `TBD`; в карте только гипотезы |
| Искать себестоимость в WB API | COGS — из `SKU_MASTER` / `COST_HISTORY` / `BUNDLES` |
| Показывать рекламу по SKU как факт без связки `date + nmId + advertId + spend` | Нарушает правило `CLAUDE.md` / `stabilization_plan.md`; до подтверждения — оценка/кабинет |
| Распределять неотнесённые расходы вручную без утверждённого правила | Способ учёта фиксируется отдельным решением |
| Закрывать пробелы покрытия Excel/Drive в обход политики | Непокрытый блок фиксируется как `TBD`, а не подменяется legacy-источником |
| Менять код, создавать функции или новые листы под эту карту | Карта — инвентаризация; изменения — отдельными согласованными шагами |
| Фиксировать Excel/Drive-цифры как окончательный эталон | Эталон подтверждается на API-only периоде |

---

## 10. Следующий безопасный шаг после заполнения карты

1. Провести **фактическую инвентаризацию** WB API по блокам §4: подтвердить точный метод/версию, детализацию и наличие ключей (`srid`, `nmId`, `advertId`); заменить `TBD` на «доступно / частично / недоступно» рядом со статусами. Только проверка, без изменения кода.
2. Отдельно проверить **связку рекламы** `date + nmId + advertId + spend` в `RAW_WB_ADS` и сверить `sum(spend)` ↔ удержание «WB Продвижение» в `RAW_WB_FINANCE`; зафиксировать `delta`.
3. Проверить **полноту внутренних справочников** (`SKU_MASTER`, `COST_HISTORY`, `BUNDLES`) на активных SKU — для COGS на уровне ЮНИТ / P&L.
4. Для покрытых блоков снять API-данные за контрольный период и сверить; результат сверки — основа будущего `docs/control_values.md` (фиксируется только после подтверждения по API).
5. Записать итоги (покрытие, пробелы `TBD`, классификацию расхождений, решения по неотнесённым расходам) в `CHANGELOG.md`. Excel/Drive-модули пометить legacy, не удаляя.
6. После согласования карты использовать `docs/wb_api_coverage_map.md` как рабочую основу для проверки API-покрытия и будущего `docs/control_values.md`.

---

*Документ фиксирует карту покрытия WB API в рамках целевой модели API-first / API-only. Кода не меняет, новых функций не создаёт, покрытие WB API не утверждает до фактической проверки. Все непроверенные позиции — `TBD / требует проверки`. Себестоимость — только из внутренних справочников, не из WB API.*
