# Архитектура EVETIS Analytics v2

> Единый справочник модели данных. Все будущие загрузчики, витрины и экраны
> **реализуют** эту архитектуру, а не определяют её по ходу. Меняем документ —
> потом код, не наоборот.
>
> Зафиксировано: 2026-07-16. **Ревизия 2** (16.07) — по замечаниям аудита:
> уточнён статус рекламы/финансов, введена двухуровневая модель товара
> (`nm_id` ↔ `internal_sku`), история остатков вынесена в отдельный FACT,
> введена корректная терминология атрибуции рекламы, зафиксированы разные
> денежные базы.

---

## 0. Что мы строим

Не набор загрузчиков и не «отчёты по Wildberries», а **собственную BI-систему
управления бизнесом EVETIS** (свой аналог Power BI/Looker под наш бизнес).
Цель: утром открыл одну страницу — за минуты понял, что происходит с продажами,
рекламой, запасами и финансами, и что нужно сделать сегодня.

Система делится на **четыре продуктовых домена** (§11): Продажи, Маркетинг,
Финансы, Операции. Каждый — законченный модуль со своими витринами и дашбордом,
но все стоят на одной модели данных.

---

## 1. Принципы

- **BigQuery-first.** Google Sheets — только «пульт»: справочники, настройки,
  API-ключи, журналы. Не хранилище и не расчётный слой.
- **Поток данных:** `WB API → Apps Script (только загрузчики/оркестрация) →
  RAW (append-only, неизменяемая история) → VIEW (дедуп/каноническое состояние) →
  REF (справочники из Sheets) → MART (готовые бизнес-показатели) → Dashboard`.
- **Conformed dimensions** — единые ключи во всех витринах. Без них drill-down
  «клик по товару → продажи/реклама/остатки/финансы» невозможен (§3).
- **Fail-closed загрузчики**, каноничность — во VIEW, снапшоты — через manifest.
- Не усложнять; изменения минимальны/обратимы/проверяемы; **PNL без контрольных
  сумм не трогаем**; рекламу не считаем поартикульной себестоимостью без связки
  `SKU→расход`; всё пользовательское — по-русски.

---

## 2. Слои и текущий статус

| Слой | Что это | Статус |
|---|---|---|
| **1. Data Platform** | RAW + VIEW + автоматический сбор из API | частично (см. ниже) |
| **2. REF** | справочники Sheets → BQ (`REF_*`) | ⬜ не начато |
| **3. Модель данных** | этот документ | 🟢 зафиксирована |
| **4. MART** | готовые бизнес-показатели | ⬜ |
| **5. Dashboard** | экраны (v0.5 → директорский → карточка товара) | ⬜ |

**Data Platform по потокам (точный статус, без завышения):**

| Поток | RAW / VIEW | Автоматизация | Статус |
|---|---|---|---|
| Заказы | `RAW_WB_ORDERS` / `V_WB_ORDERS` | watermark + hourly-триггер | ✅ **production** |
| Продажи + возвраты | `RAW_WB_SALES_RETURNS` / `V_WB_SALES_RETURNS` | watermark + hourly-триггер | ✅ **production** |
| Ночная пересверка продаж | — | daily ~04:20 МСК, окно 7 дней | ✅ **production** |
| Остатки | `RAW_WB_STOCKS` + manifest `WB_STOCKS_SNAPSHOTS` / `V_WB_STOCKS_CURRENT` | daily-снимок ~06:30 МСК | ✅ **production** |
| Реклама | BQ-слой и историческая загрузка (`V_ADV_*`, fullstats ~90д) **частично готовы** | ⚠️ **регулярная production-загрузка НЕ автоматизирована** | 🟡 не production |
| Финансы | `RAW_WB_FINANCE` / `V_WB_FINANCE` существуют, исторические данные загружены (с 05.09.2024) | ⚠️ **ежедневный production-инкремент ещё НЕ принят** | 🟡 не production |

Уровень 1 считается закрытым только когда реклама и финансы работают сами.

---

## 3. Единые ключи (conformed dimensions) — сердце модели

### 3.1. Товар — ДВА уровня, а не один

Ключевое архитектурное решение. Нельзя сводить всё к `internal_sku`: реклама,
остатки, позиции и кампании работают именно с **карточкой маркетплейса**.

```
nm_id          — конкретная карточка Wildberries (листинг)
internal_sku   — внутренний товар EVETIS
```

| DIM | Ключ | Смысл |
|---|---|---|
| `DIM_LISTING` | `marketplace` + `nm_id` | карточка маркетплейса → `internal_sku` |
| `DIM_PRODUCT` | `internal_sku` | внутренний товар EVETIS |

**Правило: во ВСЕХ FACT-таблицах храним ОБА ключа — `nm_id` и `internal_sku`.**
Иначе при нескольких карточках одного продукта, новых карточках, наборах или
переносах рекламы аналитика начнёт смешивать разные сущности.

- Агрегируем по `internal_sku` — прибыль, себестоимость, общая аналитика товара.
- Работаем по `nm_id` — реклама, остатки, позиции, кампании, карточка.
- `marketplace` заложен на будущее (планируется Ozon); сейчас всегда `WB`.

**Наборы (bundles)** — различаем два разных SKU:

```
sellable_sku   — то, что реально продано (набор)
component_sku  — компоненты, по которым раскладывается себестоимость
```

**Выручка остаётся на проданном наборе (`sellable_sku`), себестоимость
раскладывается по компонентам** через `REF_BUNDLES`.

### 3.2. Дата

Бизнес-день по Москве. Каждый факт приводит свою дату к дню: продажи `sale_dt`,
заказы `order_dt`, реклама `date`, остатки `snapshot_date`, финансы `rr_date`.

### 3.3. Склад

⚠️ Трещина, которую надо закрыть: у остатков ключ — `warehouse_id` (INT,
стабильный) + `warehouse_name` + `region`; у продаж склад — только
`warehouse_name` (строка, ~23 имени), `warehouseId` **нет**. Мост
`sales_name → warehouse_id` живёт в `REF_WAREHOUSES`; несматченные имена
заносим вручную.

### 3.4. Кампания

`advertId` → тип/имя (`DIM_CAMPAIGN`).

### 3.5. Природа факта во времени

*Событие* (заказы/продажи/финансы/реклама-день) против *снимка* (остатки —
единственный snapshot-факт).

---

## 3A. Денежные базы — не смешивать

Слово «выручка» слишком общее. Фиксируем разные базы; на одном экране они
**не смешиваются**, каждая метрика явно указывает свою базу.

| База | Что это | Источник | Где применяется |
|---|---|---|---|
| `order_amount` | сумма заказов | `V_WB_ORDERS` | воронка заказов |
| `sale_amount` | сумма состоявшихся продаж | `V_WB_SALES_RETURNS` | продажи, операционка |
| `for_pay` | база к перечислению продавцу | продажи / финотчёт | PNL |
| `ad_attributed_revenue` | выручка, которую **рекламный API приписал рекламе** | рекламный API | ROAS/ДРР рекламы |
| `finance_revenue` | сумма по финансовому отчёту | `V_WB_FINANCE` | PNL, факт денег |

Основная операционная воронка: `Заказы → order_amount`, `Продажи → sale_amount`,
`Реклама → ad_attributed_orders / ad_attributed_revenue`, `PNL → финотчёт и for_pay`.

---

## 3B. Атрибуция рекламы — корректная терминология

**Нельзя** считать `общие заказы − рекламные заказы = органические заказы` без
доказанного контракта сопоставления. Рекламный API учитывает заказ по своему
**окну атрибуции**, а основной API — по дате создания заказа: один и тот же заказ
может попасть в разные дни.

Используем термины:

```
attributed_ad_orders    — заказы, атрибутированные рекламой WB
non_attributed_orders   — остаток после сопоставления
```

Называть `non_attributed_orders` «органикой» можно **только после доказанного
контракта сопоставления** (probe + сверка окон атрибуции). До этого — не называем.

---

## 4. REF — справочники (Sheets → BQ)

Отдельный небольшой модуль REF-sync (идемпотентный, по расписанию/кнопке).
Пока справочники живут в «пульте» — до этого слоя MART не посчитает маржу/PNL.

| Таблица | Ключ | Грейн | Источник (лист) | Назначение |
|---|---|---|---|---|
| `REF_SKU_MASTER` | `marketplace`+`nm_id` → `internal_sku` | 1 строка = карточка | `SKU_MASTER` | питает `DIM_LISTING`/`DIM_PRODUCT` |
| `REF_COST_HISTORY` | `internal_sku` + дата | дата-эффективная | `COST_HISTORY` | себестоимость по датам |
| `REF_BUNDLES` | `sellable_sku` → `component_sku` | набор → компоненты | `BUNDLES` | раскладка cogs наборов |
| `REF_WAREHOUSES` | `warehouse_id` | 1 строка = склад | новый / из остатков | мост `sales_name↔warehouse_id`, регион |
| `REF_SUPPLIERS` | — | — | — | опционально |

---

## 5. RAW / VIEW — источники фактов (существующее)

| Объект | Грейн | Ключ | Замечание |
|---|---|---|---|
| `V_WB_ORDERS` | событие заказа | срез заказа | change-feed по `lastChangeDate` |
| `V_WB_SALES_RETURNS` | событие продажи/возврата | `sale_id` | есть `warehouse_name`, `nmId`, цены (`for_pay`, `finished_price`, `price_with_disc`, `total_price`, `spp`) |
| `RAW_WB_STOCKS` + `WB_STOCKS_SNAPSHOTS` | snapshot × nm_id × chrt_id × warehouse_id | manifest со статусом | **история** снимков; валиден только `status='COMPLETE'` |
| `V_WB_STOCKS_CURRENT` | последний COMPLETE-снимок | — | только текущее состояние |
| `V_WB_FINANCE` | строка отчёта реализации (`rrd_id`) | `rrd_id` | все удержания; детализация по `nmId` |
| `V_ADV_*` | day × campaign × nm_id (fullstats) | date+advertId+nm_id | расход/показы/клики/атрибутированные заказы |

---

## 6. FACT-слой (проектируемое)

Факты — тонкие вью/витрины над VIEW+REF, приведённые к conformed-ключам.
**Каждый FACT несёт и `nm_id`, и `internal_sku`.**

| FACT | Грейн | Источник | Ключевые меры | Статус |
|---|---|---|---|---|
| `FACT_SALES` | day × nm_id × internal_sku × warehouse_id | `V_WB_SALES_RETURNS` + `REF_WAREHOUSES` + `REF_SKU_MASTER` | продано шт, `sale_amount`, `for_pay`, возвраты | ⬜ |
| `FACT_ORDERS` | day × nm_id × internal_sku | `V_WB_ORDERS` | заказы шт, `order_amount` | ⬜ |
| **`FACT_STOCKS_SNAPSHOT`** | snapshot_date × nm_id × chrt_id × warehouse_id | **`RAW_WB_STOCKS` JOIN manifest `WHERE status='COMPLETE'`** | остаток, в пути к/от клиента | ⬜ |
| `FACT_FINANCE` | day × nm_id × internal_sku | `V_WB_FINANCE` | комиссия, логистика, хранение, эквайринг, штрафы, `finance_revenue`, `for_pay` | ⬜ |
| `FACT_ADS_DAILY` | day × campaign × nm_id | `V_ADV_*` fullstats | расход, показы, клики, `attributed_ad_orders`, `ad_attributed_revenue` | ⬜ |
| `FACT_ADS_KEYWORDS` 🟡 | day × campaign × keyword (× nm_id?) | **probe нужен** | показы, CTR, CPC, расход, заказы | ⬜ |
| `FACT_ADS_BIDS` 🟡 | запрос × ставка | **probe нужен** | ставка, расход, показы, позиция | ⬜ |

**Важно по остаткам:** исторический факт строится по **всем подтверждённым
(COMPLETE) снимкам** из `RAW_WB_STOCKS`, а не из `V_WB_STOCKS_CURRENT`. Только так
считаются «остаток вчера», динамика запасов, оборачиваемость на дату и анализ
поставок. `V_WB_STOCKS_CURRENT` используется исключительно для текущего состояния.

🟡 = связку `запрос/ставка → расход → SKU` сначала прощупываем probe. Если API её
не даёт — витрину не строим (правило CLAUDE.md).

---

## 7. DIM-слой

| DIM | Ключ | Атрибуты |
|---|---|---|
| `DIM_LISTING` | `marketplace` + `nm_id` | `internal_sku`, `barcode`, `vendorCode`, `chrt_id`/размер, статус карточки |
| `DIM_PRODUCT` | `internal_sku` | наименование, категория/предмет/бренд, признак набора (`is_bundle`) |
| `DIM_WAREHOUSE` | `warehouse_id` | `warehouse_name`, `sales_name`, `region`, признак агрегатного склада |
| `DIM_DATE` | `date` | день/неделя/месяц/год, день недели, флаг выходного |
| `DIM_CAMPAIGN` | `advertId` | тип (авто/поиск/каталог/…), имя, статус |

---

## 8. MART — каталог витрин

**Определения метрик (фиксируем один раз, с явной денежной базой):**
CTR = клики/показы; CPC = расход/клики; CPM = расход/показы×1000;
CR = `attributed_ad_orders`/клики;
**ROAS (рекламный)** = `ad_attributed_revenue` / расход;
**ДРР (общий)** = расход / `sale_amount` — доля рекламы в общей выручке;
ACOS = расход / `ad_attributed_revenue` (в %);
дней запаса = остаток / средние продажи в день;
маржа = `for_pay` − cogs − комиссия − логистика − хранение − эквайринг − реклама
(поартикульно только при доказанной связке).
На экране всегда подписываем, какая база используется.

### Уровень 2 — бизнес-витрины
| MART | Грейн | Входы | Показатели |
|---|---|---|---|
| **`MART_SKU_DAILY`** (якорь) | day × internal_sku (+ разрез `nm_id`) | FACT_SALES/ORDERS/STOCKS_SNAPSHOT/FINANCE + REF_COST_HISTORY | заказы, продажи, `sale_amount`, возвраты, остаток, продажи/сутки, дней запаса, cogs, маржа |
| `MART_SALES_DAILY` | day | FACT_SALES/ORDERS | заказы, продажи, возвраты, `order_amount`, `sale_amount` |
| `MART_SALES_BY_WAREHOUSE` | day × nm_id × warehouse | FACT_SALES | продано, возвраты, выручка по складу |
| `MART_STOCKS_CURRENT` | nm_id × warehouse | **последний COMPLETE-снимок** | остаток, в пути, дата снимка |
| `MART_TURNOVER` | internal_sku (× warehouse) | FACT_SALES + FACT_STOCKS_SNAPSHOT | продажи/сутки, остаток, дней запаса |
| `MART_SUPPLY_PLAN` | internal_sku | MART_TURNOVER | дней осталось, дата окончания, рекомендуемая поставка, приоритет |

### Уровень 3 — рекламные витрины
| MART | Грейн | Показатели |
|---|---|---|
| `MART_ADS_OVERVIEW_DAILY` | day | расход, показы, клики, CTR, CPC, CPM, `attributed_ad_orders`, продажи, ДРР, ROAS, ACOS |
| `MART_ADS_CAMPAIGN_DAILY` | day × campaign | расход, показы, CTR, CPC, клики, `attributed_ad_orders`, `ad_attributed_revenue` |
| `MART_ADS_SKU_DAILY` | day × nm_id (+ `internal_sku`) | продажи, `attributed_ad_orders` vs `non_attributed_orders`, CTR, CR, ср. позиция, расход, ДРР, ROAS |
| `MART_ADS_KEYWORD_DAILY` 🟡 | day × keyword | показы, CTR, CPC, расход, заказы, выручка, ДРР, ROAS, ставка, позиция |
| `MART_ADS_BID` 🟡 | запрос × ставка | ставка, расход, показы, CTR, заказы, конверсия |

### Финансовые витрины
| MART | Грейн | Показатели |
|---|---|---|
| `MART_PNL_SKU_DAILY` | day × internal_sku | `finance_revenue`/`for_pay` − удержания − cogs − реклама = прибыль, маржа% |
| `MART_PNL_TOTAL` | день/неделя/месяц | сводный PNL (⚠️ контрольные суммы обязательны) |

---

## 9. Связи (управленческий куб)

```
        DIM_DATE ──────────────────────────┐
        DIM_LISTING (marketplace+nm_id) ─┐  │
        DIM_PRODUCT (internal_sku) ──────┤  │
        DIM_WAREHOUSE ───────────────────┤  │
        DIM_CAMPAIGN ────────────────────┤  │
                                          ▼  ▼
 FACT_ORDERS  FACT_SALES  FACT_STOCKS_SNAPSHOT  FACT_FINANCE  FACT_ADS_*
        └──────────┴───────────┴───────────────┴──────────────┘
                              ▼
                 MART_SKU_DAILY  (день × товар — центр куба)
                              ▼
  MART_TURNOVER · MART_SUPPLY_PLAN · MART_ADS_SKU_DAILY · MART_PNL_SKU_DAILY
                              ▼
                          Dashboards
```

Всё сходится по паре ключей `nm_id` / `internal_sku` × день. Клик по товару на
дашборде = фильтр, который «подсвечивает» все факты сразу.

---

## 10. Дашборды

| Экран | Что показывает | Витрины-источники | Когда |
|---|---|---|---|
| **Dashboard v0.5** (продажи + операции + **базовая рекламная воронка**) | показы, клики, CTR, CPC, расход, общие заказы, `attributed_ad_orders`, продажи, ДРР, ROAS; остатки, дней запаса, SKU с риском дефицита; таблица по товарам; таблица по кампаниям | MART_SALES_DAILY, MART_SKU_DAILY, MART_STOCKS_CURRENT, MART_ADS_OVERVIEW/CAMPAIGN/SKU_DAILY | Этап 5 |
| **Экран директора** | + прибыль/финансы, план поставок, прибыль по категориям | + MART_PNL_*, MART_SUPPLY_PLAN | после финансов |
| **Карточка товара** (drill-down) | продажи, остатки, финансы, все кампании, атрибуция, запросы, ставки, позиции, история, динамика | все MART по фильтру `nm_id`/`internal_sku` | Этап 7 |

Поисковые запросы, ставки, позиции и глубокая карточка — во второй версии, после probes.

---

## 11. Четыре продуктовых домена

| Домен | Что внутри | Витрины/факты |
|---|---|---|
| **1. Продажи** | заказы, продажи, возвраты, склады | FACT_SALES/ORDERS, MART_SALES_*, MART_SALES_BY_WAREHOUSE |
| **2. Маркетинг** | реклама, атрибуция, SEO, позиции, запросы | FACT_ADS_*, MART_ADS_* |
| **3. Финансы** | прибыль, комиссии, логистика, ДРР, ROI | FACT_FINANCE, MART_PNL_* |
| **4. Операции** | остатки, оборачиваемость, поставки, прогноз дефицита | FACT_STOCKS_SNAPSHOT, MART_TURNOVER, MART_SUPPLY_PLAN |

---

## 12. Открытые вопросы, зависимости, риски

- **Атрибуция рекламы** — доказать контракт сопоставления (окно атрибуции
  рекламного API vs дата создания заказа) прежде чем называть остаток «органикой».
- **Реклама по запросам/ставкам** — probe до проектирования: даёт ли API связку
  `запрос/ставка → расход → SKU`.
- **Реклама vs финансы** — `fullstats` = управленческий поартикульный расход;
  финотчёт = недельная сумма списаний (НЕ поартикульно). Не смешивать.
- **Склад** — сверить ~23 имени продаж с `warehouse_id` остатков, завести
  `REF_WAREHOUSES`, решить судьбу несматченных.
- **Наборы** — раскладка `sellable_sku → component_sku` для cogs; выручка на наборе.
- **Себестоимость** — `REF_COST_HISTORY` дата-эффективная.
- **Несколько карточек одного товара** — `DIM_LISTING` должен корректно
  схлопывать несколько `nm_id` в один `internal_sku`.
- **Данные** — 1 unmatched `nm_id` в остатках (`1083392113`) внести в `SKU_MASTER`.
- **Ёмкость Google Sheets** на пределе — RAW из Sheets убирать после аудита.

---

## 13. Порядок реализации (утверждён 16.07.2026, ревизия 2)

| # | Этап | Статус |
|---|---|---|
| 1 | Исправить и закоммитить Architecture v2 (отдельный PR) | 🟢 текущий |
| 2 | **REF Sync** — `REF_SKU_MASTER`, `REF_COST_HISTORY`, `REF_BUNDLES`, `REF_WAREHOUSES` | ⬜ следующий кодовый |
| 3 | **Реклама → production**: ежедневная статистика кампаний, `advertId × nm_id × date`, расходы/показы/клики/атрибутированные заказы, кампании и типы, триггер, пересверка последних дней | ⬜ |
| 4 | **Базовый набор MART**: `MART_SALES_DAILY`, `MART_SKU_DAILY`, `MART_STOCKS_CURRENT`, `MART_ADS_OVERVIEW_DAILY`, `MART_ADS_CAMPAIGN_DAILY`, `MART_ADS_SKU_DAILY` | ⬜ |
| 5 | **Dashboard v0.5** — с базовой рекламной воронкой (не без рекламы) | ⬜ |
| 6 | **Финансы → production + полноценный PNL** | ⬜ |
| 7 | Запросы, ставки, позиции, глубокий drill-down (после probes) | ⬜ |

REF Sync первым: ограничен по объёму, нужен почти всем витринам и закрывает
единые ключи. Реклама раньше дашборда — потому что рекламная воронка сейчас
главный управленческий вопрос.

---

## 14. Инварианты (неизменны)

RAW append-only; VIEW каноничен; снапшоты — через manifest + статус COMPLETE
(история строится только по COMPLETE); загрузчики fail-closed; conformed-ключи
(`nm_id` **и** `internal_sku`) обязательны во всех фактах; денежные базы не
смешиваются; нет поартикульной рекламной себестоимости без связки `SKU→расход`;
«органика» — только после доказанной атрибуции; `PNL_TOTAL` без контрольных сумм
не трогаем; всё пользовательское — по-русски.
